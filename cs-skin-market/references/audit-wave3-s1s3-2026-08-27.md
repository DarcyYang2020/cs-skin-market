# ③独立审计 · Wave3 模拟盘 S1–S3 落地（2026-08-27）

**审计对象**：研发 Wave3 S1–S3 落地（decision-log DS + roadmap v82 Wave3 卡 + commit 1424721）
**审计员**：窗口③（独立审计 / 刹车）
**红线执行**：只认 roadmap v82 Wave3 卡预注册判据/验收 + 代码/报告/产物事实；DS 自述仅对照。**独立重跑冒烟 144 passed / 0 failed / 0 skipped**。

---

## 一、逐项核验

### S1 悠悠有品采集可行性预研 —— 通过
- **预注册 §4.4 全达成**：①Playwright headless + response 拦截（4 轮探针 `yyyp_probe_{feasibility,detail,detail2,route}.py`）✅；②可行性报告 `yyyp-feasibility-2026-08-27.md`（可采字段/历史深度/反爬风险/频率）✅；③不落库、不动生产（探针只读）✅。
- **结论证据可复核**：在售列表可采——`pc-api.youpin898.com/api/homepage/pc/commodity/page` 200、totalElements≈11354、字段 id/commodityName/commodityHashName/iconUrl、历史深度=当前快照无历史；求购/成交未实证——用户级 API 全返 84101 登录墙 + `/goods/{id}` 404 语义 + SPA 未触发求购/成交 API；反爬 deviceW2。
- **挂账（待用户拍板，非预研缺口）**：求购/成交需「悠悠有品登录账号」方可继续验证——预研结论本身完整（可采性判断成立、受阻原因实证），登录账号属外部资源决策。
- 验收：①报告 ✅；②在售可采部分已给 API 端点证据（S2 数据源候选）✅；③不动生产 ✅。

### S2 模拟盘台账 + 交易域表 —— 通过
- **表结构**：paper_account/paper_positions/paper_trades/**paper_orders/paper_fills**/paper_baseline 六表齐（orders/fills 为 S2 新增，幂等建表+迁移）✅。
- **§4.3 成交规则**：买=底价在售 0 费（PAPER_FEES buy_pct=0.0）✅；卖=必须有货 1% 费（settle_exits 无货/数量不足 → 拒单落 paper_orders status=rejected，sell_pct=1.0）✅；费率与 E2 对齐 ✅；不自动下单 ✅；与真实库存可选同步（PAPER_SYNC_REAL_INVENTORY 默认关）✅。
- **现金 bug 修复（S2 新用例暴露）**：`settle_exits` 现金回补由 `qty×(px/entry)×(1−费)` 改为 **`qty×px×(1−费)`**——数学正确（原式多除 entry 导致少回补 entry 倍）；**只处理 `closed=0` 当前持仓 → 前向生效、历史 paper_trades 不变**；修复已登记 DS、模拟盘层变更不 bump ENGINE_VERSION（非引擎信号逻辑）✅。

### S3 钉钉通知闭环 —— 通过（附 2 项待环境验证登记）
- **预注册 §4.2 四项**：①意向单结构（`intention_card` 品/方向/数量/参考价/理由/期望/风控标签 7 字段）✅；②钉钉卡片推送（`push_intention` 复用 `notify_alert.route_alert` level=trade，**kill switch notify 自动拦截**）✅；③回报入口（`report_fill`：buy 建仓现金扣减 / sell 平仓减仓现金回补 qty×px×(1−卖费)、**重复回报拒**（回报后 status 离开 intention）、卖出无货拒单）✅；④kill switch 可暂停出单/通知 ✅。
- **未回报超时接线 O1**：ops.py ⑥ 新增「S3 回报超时」检查（`unreported_orders` 超时 24h → WARN，表未就绪忽略）——O1「判据先行」随 S3 落地转为实际检查，符合预期演进 ✅。
- 冒烟 144/0/0 ✅。
- **待环境验证（登记，非缺口）**：①钉钉真实送达需生产 webhook 环境（dry_run 已验证卡片格式与路由）；②O1 回报超时在自动镜像模式恒 PASS（意向单即时 filled），手动回报模式生效——行为差异合理（自动镜像无未回报概念），需实际运行确认。

## 二、裁定

- **S1/S2/S3 全部通过**：预注册判据逐项达成、代码/报告证据可复核、冒烟独立重跑 144/0/0。S2 现金 bug 修复正确且前向生效；S3 闭环（意向单→钉钉→回报→台账）完整接线 O1。
- **登记 3 项（非缺口）**：①S1 求购/成交挂账「是否提供悠悠登录账号」待用户拍板；②S3 钉钉真实送达待生产 webhook 环境验证；③O1 回报超时自动镜像 vs 手动回报行为差异待实际运行确认。
- 不 bump ENGINE_VERSION（模拟盘层）✅；S1 探针只读不落库、不动生产 ✅。

---

**一句话**：Wave3 S1–S3 全部通过——S1 预研完整（在售可采实证/求购成交登录墙 84101+404 实证，挂账登录账号待拍板）；S2 交易域六表+§4.3 规则+现金回补 bug 修复（公式正确、前向生效）；S3 意向单→钉钉→回报闭环接线 O1（kill switch 拦截/重复回报拒/无货拒单齐）；冒烟独立重跑 144/0/0；3 项待环境验证登记（钉钉真实送达/自动镜像 vs 手动回报）。
