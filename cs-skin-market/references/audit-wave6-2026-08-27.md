# ③独立审计 · Wave6 运维层 O1–O4 落地（2026-08-27）

**审计对象**：④运维 Wave6 O1–O4 落地（decision-log DD 1859 条目 + roadmap v82 Wave6 卡 965–987 行）
**审计员**：窗口③（独立审计 / 刹车）
**红线执行**：只认 roadmap v82 卡内**预注册判据**（O1 监控项/O2 运维端+自动急停/O3 审计/O4 三级路由）+ **代码事实**；DD 自述仅作对照。独立重跑冒烟 `tests/test_smoke.py`（系统 python 3.11）：**137 passed / 0 failed / 0 skipped** ✅。

---

## 一、四点复核

### ① kill switch 状态机（全局/策略级、auto 只开不恢复）—— 通过 ✅
- **状态结构**：`data/ops_kill_switch.json` = `{global, strategies:{paper,notify}, auto:{rule:ts}, updated_at, by, reason, decision_log_ref}`；全局闸停拦截一切、策略级独立（`is_blocked` 语义正确）。
- **auto 只开不恢复**：`auto_escalate()` 四条件（drawdown/rejects/stale_data/collect_gate → scope=paper）**只传 blocked=True**，解除仅能人工（CLI/webapp `set_kill_switch(False)`）——防震荡成立；总开关 `OPS_RULES.kill_switch.auto=False` 时仅记录不闸停。
- **独立于业务链**：`ops_tool.py` CLI 直读写状态文件 + `/api/ops/kill-switch`（webapp 卡死仍可触发）；`_atomic_write` tmp+replace 原子写。
- **联动**：paper_trading 建仓闸停（冒烟 t_ops_paper_gate 验证）+ notify 拦截（route_alert）+ 变更自动落 O3 审计（config_audit 含 decision_log_ref）+ log_event("kill")。
- 轻微建议（非阻断）：①手动解除后 `auto` 记录残留不清空（状态文件同时显示 auto{rule} 与 strategies=False，建议解除时清 auto 或标注解除 ts）；②自动急停只作用于 paper（notify 不自动停，靠 O4 手动/全局拦截）——通知不含资金动作，属合理设计选择，注明即可。

### ② O4 三档路由与 D3–D5 联动 —— 部分通过 ⚠️
- **route_alert 三档**：`levels=[collect,quality,trade]` + 标签前缀（采集/质量/交易）+ **每级独立 webhook env 覆盖**（`webhook_env`：quality=NOTIFY_WEBHOOK_QUALITY / trade=NOTIFY_WEBHOOK_TRADE，缺省走基础 NOTIFY_WEBHOOK_URL = 分级不漏报）✅；notify 拦截留痕、推送/拦截/失败全写 ops_log ✅；CLI `--level` + `/api/ops/*` 接入 ✅。
- **联动实况**：
  - **D3 采集闸门 ✅**：`run_ops_monitor(health_fail=健康检查 FAIL 数)` → quality 告警 + 自动急停；O1 `stale_data_days=2` 与 `CLEANING_RULES.freshness.kline_stale_days=2` 显式对齐。
  - **O1/O2 ✅**：trade 档告警来自 run_ops_monitor 的 FAIL 项（对账/回撤/拒单）；kill switch(notify) 拦截。
  - **D4 cleaning_ledger ❌ 未接入**：`pipeline/cleaning_ledger.py` 仅在采集侧写入（run_daily_collect:113），ops/notify **无任何读取消费**——监控/告警不读清理台账。
  - **D5 备份新鲜度 ❌ 未实现**：备份功能存在（每日备份 + /api/backup/*），但「备份新鲜度/备份失败检查」**全库零命中**。
- **判定**：O4 验收标准②「与 D3/D4/D5/O1/O2 联动」**部分未达**（D3/O1/O2 ✅、D4/D5 ❌）。roadmap 状态行与 DD「cleaning_ledger/备份新鲜度检查已预留接口 / Wave1 落地后自动生效」**表述超前于代码事实**——代码中无预留接口（无函数桩/TODO/消费点），仅有数据格式同构。**须登记为显式待办，不得沿用「已预留」表述**。

### ③ O1 对账/回撤口径 —— 通过 ✅（附边界注明）
- **对账**：status.json `equity` vs 实库重算（`cash + Σ open qty × 最新价`，读 paper_account/paper_positions + price_history 最新价），阈值 1.0%；无 status.json → WARN 跳过（S2 未上线不误报）。
- **回撤**：自峰值口径，峰值台账 `ops_paper_peak.json`（eq > peak 时更新）；dd ≥ 15% → FAIL + 自动急停(paper)。
- **连续拒单**：当日 buy 信号 − 当日已建仓 ≥ 3 → FAIL + 急停；**数据源新鲜度**（最新 K 线距今 ≥2 天）与**采集闸门**（health_fail>0）→ FAIL + 急停。
- 边界注明（非阻断）：①S2 台账表已建但无建仓、当日 buy 信号 ≥3 时「连续拒单」会 FAIL + 自动急停——模拟盘未正式启用即触发，属保护性行为，运维须知晓；②实库重算 cash 兜底 1,000,000（表缺失时），status.json 有 equity 时差异会偏大——表缺失本身即状态不一致，FAIL 提醒合理。

### ④ S3 回报超时判据先行 —— 通过 ✅
- `config.OPS_RULES.trade_monitor.report_timeout_hours=24` 已预注册（PARAM_REGIME 参数台账已登记，config.py:385 出处段）；run_ops_monitor ⑤ S3 闭环监控项**恒 WARN 登记**（Wave3 S3 未上线不误报），阈值已锁，S3 上线后自动生效——「判据先行」语义正确。

---

## 二、裁定

- **① ③ ④ 通过**；**② 有条件通过**（D3/O1/O2 联动成立，**D4 cleaning_ledger 监控消费 / D5 备份新鲜度检查未实现**——O4 验收②未完全达成）。
- 冒烟独立重跑 **137/0/0** ✅（6 个 Wave6 用例全 PASS：O2 状态机+审计+独立文件 / O3 审计四要素+检索 / O4 三档+notify 拦截 / O1 对账 FAIL+告警 / O2 自动急停 / O2 联动建仓闸停）。
- 阈值登记、PARAM_REGIME 台账、不进引擎决策、不 bump ENGINE_VERSION——全部符合。

## 三、须②/④登记的事项

1. **D4/D5 联动缺口显式登记为待办**（监控消费 cleaning_ledger + 备份新鲜度检查），roadmap/DD 表述更正为「未实现，待补」；补齐后 O4 验收②才完整达成。
2. **decision-log 编号重复**：1847 与 1859 均为「DD」（R1 冻结 / Wave6 落地）——后续条目须编号唯一。
3. 轻微建议（①a/①b/③边界）按运维惯例注明即可，不阻断。

---

**一句话**：kill switch 状态机（全局/策略级、auto 只开不恢复）、O1 对账/回撤口径（status vs 实库 1% / 自峰值 15%）、S3 判据先行（24h 登记 WARN）三项通过，冒烟独立重跑 137/0/0；O4 三档路由本体正确，但 **D4 cleaning_ledger / D5 备份新鲜度联动未实现**（roadmap「已预留接口」表述超前），须登记为显式待办后 O4 验收②才完整。
