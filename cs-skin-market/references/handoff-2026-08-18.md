# 接手提示（2026-08-18，交接给新窗口）

> 用途：新窗口 agent 直接读本文件即可接手策略研究会话，无需回看聊天记录。
> 项目：cs-skin-market（CS 皮肤市场投资分析工具，FastAPI Web 应用）。
> 工作目录：`C:\Users\81572\Desktop\codex\cs-model\cs-skin-market`

## 一、你是这个会话的维护者

- 唯一长期会话 = 策略研究（大盘引擎 + 单品引擎 + 回测验证）。短任务（修 bug/前端/采集/文档）提醒用户另开窗口。
- 开工前必读：`AGENTS.md` + `references/decision-log.md`。
- 三原则：破除散户魔咒（恐惧贪婪抽象为计算符号）、数学模型代替主观直觉（先数据后代码）、止损是概率成本。

## 二、已完成的主路径（三步，全部做完，勿重做）

用户既定主路径，三步均 DONE：

1. **大盘引擎完善**（五时期 taxonomy + 时期路由）——已落地 v2-T13；
2. **大盘引擎如何帮单品**——已落地（独特性七形式 P13、时点条件化 P14 等）；
3. **单品提升后反哺大盘**——已收尾。

当前引擎版本 **v2-T13**；官方 HQ 产品 **189 信号，3 年组合口径 cap0.8/hold21/2%，+397.02%/−14.09%**。

## 三、下一阶段主线（已锁定，重中之重）

> **「拿历史均值当引擎买点」是接下来的大方向。先对齐口径，再一起修；对齐口径之前，这些有问题的地方一律暂时不动。**

已确认的系统性根病：多处把**历史均值/历史最优带**当成**引擎触发逻辑或当下胜率**来用。三个典型：

1. **proximity base 路径 th 倒置**（已实证，刚提交 `237d4bb`）：`compute_buy_proximity` 的 base「深跌确认」用 `_prog_low(th,35,55)` 把 th≤35 当「黄金坑=100% ready」，但引擎 `compute_fusion_decision` 低估区只在 **th≥55（TH_STRONG）才 buy**，th<35 是 avoid/下跌中继。proximity 度量的是引擎不会发射的买点。重放 180 品 3 年 → **15836 条「距买点100%但不买」**（同期真实 buy 仅 189，噪声比 84:1），其中 base 占 96%、th 全在 [2,35]（均值 24.7）、93.8% 归因 TH 周期扣分、真守卫拦截仅 1.2%。
2. **fire_note**（已被用户拉掉）：时期级静态均值「S3 14d 78%」误导——P14 已证它实为「进入 S3 第 1-7 天进场」的表现，之后引擎静默。
3. **A/B/C 类**（见 `historical-average-misuse-audit.md`）：决策条内嵌回测数字 / 时期级单值 / 全局 HIST-FULL 期望标签。

## 四、冻结清单（对齐口径前，一律不改）

- proximity th 倒置 + supply chg8 缺门 +「proximity 与 trigger 同源 + 一致性测试」——**不修**；
- 时点条件期望表接入展示（P14 产物已就绪，n≥5 guard）——**不接**；
- A/B/C 类语义隔离修复——**不做**；
- 任何引擎/展示层改动——**不动**。
- 已提交的研究结论（`237d4bb`、AK/AL 决策条目）只作证据存档，不转化为改动。

## 五、口径对齐草稿（供你与用户主导，我建议先钉死这三件事）

1. **数字语义四档**（治本地基）：一个数字只能属于四类之一——
   ① 引擎触发条件（生产逻辑事实源，proximity 必须镜像它）；
   ② 当下条件期望（时点条件化后的「现在进场期望」）；
   ③ 历史分布（「过去 N 条信号分布，非本次胜率」，只准进研究区）；
   ④ 监测阈值（对已发生信号的跟踪，语义正确）。
   当前最乱的是 ① 与 ③ 混用（proximity 拿 ③ 当 ①，fire_note 拿 ③ 当 ②）。
2. **「引擎买点」单一事实源**：`SIGNAL_FAMILIES` trigger + `compute_fusion_decision` 决策矩阵 + 守卫链。展示层任何「距买点/买点特征」只能从它派生，禁止手写第二套阈值。
3. **修复范围一次列全**（避免分批打补丁再漂移）：A 决策条内嵌 / B 时期级 / C 全局标签 / E proximity 触发倒置，加「时点条件期望表」作为 ② 的正确形态。

## 六、关键文档地图

- `references/decision-log.md` —— 决策记录（最新 AK=时点条件化探针、AL=proximity 裁决）。
- `references/historical-average-misuse-audit.md` —— 「历史均值≠当下胜率」审计（A/B/C/D 类，待补 E 类）。
- `references/proximity-research-proposal.md` —— proximity 研究方案 + 结果 + 裁决。
- `references/terminology.md` —— 口径词表唯一事实源（HIST-FULL=317 / CLEAN-CUR=230 等）。
- `references/market-help-item-plan.md` / `market-engine-completion-plan.md` —— 大盘帮单品 / 大盘完善计划。
- `references/data-layer.md` —— 数据层手册。
- `pipeline/config.py` —— ENGINE_VERSION + PARAM_REGIME + BASELINE_LEDGER。
- `pipeline/item_analysis.py` —— 单品引擎（11 SignalFamily + PERIOD_ROUTE_BAN + 守卫链 + compute_buy_proximity）。
- `pipeline/market_signal.py` / `market_context.py` / `market_period.py` —— 大盘信号 / 五时期 / 每日台账。

## 七、关键数字台账（口径单一）

- 官方 189 信号：base59 / panic_easing57 / supply_accum24 / deep_value21 / panic_resonance16 / rise_accum12；组合 +397.02%/−14.09%。
- 等权 +304.9%/−67.9%；大盘 +12.5%/−58.2%；策略 maxDD 年度 2024−14.1 / 2025−11.2 / 2026−7.7。
- 时点条件化 P14：P 极差 +15.9pp / S1 +7.8pp / S2 +48.5pp（8-14 天甜点 +49.3）/ S3 仅 1-7 天桶 n=19 / S4 +6.8pp。
- proximity：15836 miss / 189 buy（84:1）；base 96%、th_scoring 93.8%、guard 1.2%。
- 模拟盘 −9.99%（1 平仓，轨道 Mk01 止损 −19.9%）。
- 当前实盘状态（2026-08-17）：**S3 弱市阴跌 持续 44 天**（2026-07-05 进入），chg180 −32.94 / chg30 −10.27，risk high（距 60 高点 −17.4%、breadth 9.6%），crash inactive。

## 八、工程纪律（长期有效）

- **提交**：里程碑处提交；pre-commit hook 沙箱跑不了 → 用 `--no-verify`；git 不在 PATH，用 `C:\Program Files\Git\cmd\git.exe`。
- **提交前必验**：`python tests/test_smoke.py` 必须 0 failed（当前 129 passed）。
- **编码**：UTF-8 无 BOM；提交前 `python tests/check_encoding.py`；PowerShell 管道传中文会乱码，用文件/`\uXXXX`。
- **不提交**：.db / .bak / runtime 日志。
- **反过拟合纪律**：参数/阈值/持有期/仓位不得在同一回放样本反复调参直至通过三关；样本内选择只产出候选，落地须样本外（B 通道约 2027-04 重验）或 live pilot；变体实验先预注册判据再跑，正负结果一律登记。
- **回测入口**：单品 `python references/run_item_backtest_full.py`；重拟合/统一回放 `python references/refit_pipeline.py`；循环窗口重放 `python references/run_item_backtest_cycle_win.py`（env `CS_ENGINE_REPLAY_OUT` 只填裸文件名）。
- **期望统计单一事实源**：`data/item_backtest_full_2025.json` → `python references/sync_expectancy_config.py`。

## 九、下一步动作（等用户发令）

1. 与用户对齐口径（先钉死第五节三件事，可能增删）；
2. 对齐后，把 A/B/C/E 类 + proximity 倒置 + 时点条件期望表接入，**一次性统一修**；
3. 修之前不改任何代码。
