# 角色定位 + 现状接替简报（2026-08-27 18:00，PM 编制）

> 用途：开新窗口接替工作。每个角色 = 定位（架构依据）+ 当前状态（截至 18:00）+ 接替动作。
> 权威事实源：`cs-quant-architecture.md`（六层 v1.0 定稿）、`decision-log.md`（DC→DW）、`iteration-roadmap.md`（v82 批次）。
> 全局红线（所有窗口共守）：①`oos_zone` 守院（验证段 ≥2025-08-10 / B 通道 ~2027-04-25 为禁区，探索只许 fit 段）②组合/权重**禁优化器**（等权/风险平价/规则）③**预注册判据先行**，跑前定死，不认自述、不照镜子调阈值（C2 教训）④③审计独立只读核验，不替改到通过 ⑤研究只出评估卡/注册表，**不立落地卡、不碰引擎**；旧路径 C1–C5（374 有偏样本）已整体作废。

> **✅ 更正（2026-08-27 18:22，PM 据④运维确认更新）**：原「唯一挂账 DJ③ D4 rebuild_derived 待补跑」**已清零**。④运维确认 DJ③ 已按 decision-log **DL(02:05)+DN(02:10)** 闭环——provenance 3 条留痕三跑幂等 PASS、`replay_cycle_win.db` 实测 items 405 / price_history 259,222 / market_index 1,015 且 integrity=ok、恢复演练台账 02:05:35 三库一致；**O4② 同判终态**。简报 DW 段「待补跑」系状态滞后，以 decision-log 为准，DJ③ 从挂账移除、无需二次补跑。剩余开放项见各窗口「接替动作」。

---

## ① PM（产品 / 项目经理）

**定位**：治理中枢。定"做什么、怎么算通过"——立卡三件套（目标 + 预注册判据 + 验收标准）、排优先级、按③审计结果对照验收、落 decision-log。红线：**不写代码、不改参数、不跑回测**。

**当前状态**：
- 治理链全闭环：DC（架构登记）/ DD（R1 判据冻结）/ DE（R1 组9 升主评）/ DI+DJ（Wave1 审计+D1 口径拍板 yyyp>buff>c5）/ DM（DJ 双头编号归口）/ DG+DF+DN（Wave6 落地+③审计+O4② 复核通过）/ DR（R3/R5 判据冻结）/ DT+DU（Wave3 审计+S1 账号等三项登记）/ DV（R3 结论）/ DW（角色状态整理+新窗口接替定稿）。
- 挂账：**无**（治理层 + 运维层 + 研发层均已闭环：DJ③ D4 rebuild 经④运维 18:22 确认已按 DL+DN 闭环移除；collect_gate 补采+解禁已闭环 O3 20:07:52；S3 ③复核已闭环 ED；备份首备已闭环；**研发 commit 已 push 闭环（remote main = 2f972b2 = 本地 HEAD）**）。🟡 **历史非阻断登记（已闭环）**：decision-log 编号撞车——④研发 S3 关键词保证条目原误用 `## EB`（2367 行），与③ Wave4 审计 `## EB`（2342 行）撞车；**④研发已改号 `## EE`（PM 20:30 核验：全库仅剩③ Wave4 一个 ## EB）**。

**接替动作**：持续治理；收口③审计（R3 审计，O4② 已随 DJ③ 闭环无需再触发）；**研发已认领 commit 并 push 闭环（remote main = 2f972b2 = 本地 HEAD，PM 20:30 经 `git ls-remote` 核验一致）**；整体 review 完成。

---

## ② 研究（算法研究窗口）

**定位**：研究层（架构 §2）。因子挖掘 / 特征工程 / 策略研究 / 模型训练。**基调**：符合 CS 品类特点、不照搬股市量化；方法论最成熟（预注册→族开回放→四关→③审计）。红线：**只做研究，不落地生产代码、不碰 `pipeline/` `webapp/` `tests/` 生产改动**。

**当前状态**：
| 卡 | 状态 | 关键结论 |
|---|---|---|
| R1 因子评估 | ✅ 完成（DD 冻结、DV 复核） | 21 因子评估卡，**无新候选**；供给 sc7/sc30/s7_ratio 条件 IC 唯一正信号；oos_zone 守院 |
| R2 因子注册表 | ✅ 落地（DP、DQ ③复核通过） | 21 因子：候选 3 / 证伪 15 / 存档 3（`data/factor_registry.json`） |
| R3 策略隔离评估 | ✅ 跑完（DV）+ ✅ ③审计通过（DX） | 6 族全部证伪、零漂移、无新族注入 → **多策略形态不成立，单引擎（融合决策）维持**（DX 四关独立复算全一致，对 G1 口径放宽与 G4 seed 选择均稳健） |
| R4 挖掘流程 | ✅ 完成 | 启动清单交付 |
| R5 内生情绪 v0 | ✅ ②执行完成（DY）+ ✅ ③审计通过（DZ，verdict 无增量证伪成立） | v0 固定权重 + 增量 IC 硬判据 + 仅加分过滤 |

**接替动作**：**R5 ②已执行完成（DY）+ ③审计通过（DZ，verdict 无增量证伪成立，增量 IC fit +0.0026 / val 复验 −0.0055 独立复算一致）**；**R1–R5 研究主线全收口 → Wave4（E1–E4）解冻，④研发可立卡**。

---

## ③ 审计（独立审计窗口）

**定位**：守门员。对所有落地/研究产物做**独立只读核验**（SQL 直查 / 读 JSON 产物 / 亲自重跑冒烟），结论全部由原始产物支撑，不认自述、不替②调参、不"帮忙改到通过"。覆盖：Wave1（DI）、Wave6（DG/DF/DN）、R2 registry（DQ）、Wave3（DT）、R3（DX 通过）、Wave4（EB 收口 + ED 复核闭环）。

**当前状态**：DI / D4 / D6 / O4②（DN）/ DF / DG / DQ(R2) / DT(Wave3) / **DZ(R5)** / **EB(Wave4)** / **ED(S3 复核 + Wave4 EC 修复复核)** **全闭环**（E1–E4 全通过；E1 幸存者门经④研发修复后③复核真通过 prod=405/eliminated=0），独立重跑冒烟 147/1/0（1 网络用例 429 限流）。
- 待办：**S3 ③复核已闭环（ED）**——代码侧 CS 前缀已生效（④研发 98b387f：route_alert `CS【{tag}】` 前缀 + intention_card 首行 `【CS 模拟盘意向单】`）+ ④实发 push_intention 证据链闭合，310000 链路风险消除。🟡 非阻断登记（移交 PM 知悉，不影响结论，均不阻塞）：①**decision-log 编号撞车（已闭环）**——④研发 S3 关键词保证条目原误用 `## EB`（2367 行），与③ Wave4 审计 `## EB`（2342 行）撞车；**④研发已改号 `## EE`（PM 20:30 核验全库仅剩③ Wave4 一个 ## EB）**；②**R5(DZ) 2 项**——a) 滚动同号月口径（②只统计 market 上下文可算日期 16 月/0.9375，审计全量 21 月/0.5238，已复现为口径选择，verdict 不受影响）；b) 产物 verdict_note 文案把「滚动已过」写成「未过」（仅文案，判定逻辑正确）；③**R3(DX) 2 项**——a) G2 双向门槛（回撤改善>10pp 亦不过，但总收益差>1000pp verdict 不受影响）；b) 冒烟 2 联网 API 用例因外部源限流失败（SKIP_NET=1 复跑 138/0/0，待限流解除复核闭环）。

**接替动作**：R3 审计已通过（DX）；**R5 审计已通过（DZ，verdict 无增量证伪成立）**；**Wave4 ③审计 EB 收口 + ED 复核闭环**（E1–E4 全通过；E1 幸存者门经④研发修复后③复核真通过）；**S3 ③复核闭环（ED，代码侧 CS 前缀已生效+④实发证据链闭合）**；O4② 已随 DJ③ 闭环，无需待补跑。

---

## ④ 研发（工程实现窗口）

**定位**：执行层（架构 §4）的代码实现方。负责数据地基 / 模拟盘 / 评估层 / 决策预留的工程落地，产出跑通 `tests/test_smoke.py` 0 failed 后移交③审计。**不碰治理决策、不改研究判据**。

**当前状态**：
| 批次 | 状态 |
|---|---|
| Wave1 D1–D7（数据地基） | ✅ ③审计通过（DI，四项登记闭环：D1 口径/D3 补用例/D4 rebuild 三跑幂等/D6 oos_guard 已接 R1） |
| Wave3 S1–S3（模拟盘） | ✅ ③审计通过（DT）；DU 三项：S1 账号**已登录可用（用户 08-26 登录+10天免登，~09-05 到期需续登；盘口采集已激活 raw_order_book/trade 今 18:08 起累积 503 行）** / S3 钉钉✅验证通过 + **③复核闭环(ED)** / O1 超时两模式待运行观察 |
| Wave4 E1–E4（评估层） | ✅ **③审计 EB 收口 + ED 复核闭环**（E1–E4 全通过；E1 幸存者门经④研发修复 EC 后③复核真通过 prod=405/eliminated=0，E4 两处归因修复） |
| Wave5 X1（策略抽象层接口）/ X2（流动性守卫） | ⏳ 挂账（X2 等盘口数据 3–6 月） |
| commit | ✅ 已认领并 push 闭环（remote main = 2f972b2 = 本地 HEAD；5 个 ops 运行时文件 `data/*.jsonl|json` 按研发意不入库，仍为 untracked） |

**接替动作**：**全工作树 commit 已认领并 push 闭环（remote main = 2f972b2 = 本地 HEAD，PM 20:30 核验）**；**Wave4 已立 + ③审计 EB 收口 + ED 复核闭环（E1–E4 全通过）**；**decision-log 编号撞车已改号闭环（S3 关键词保证条目 ## EB→## EE，PM 核验全库仅剩③ Wave4 一个 ## EB）**；Wave5 按卡推进。

---

## ④ 运维（部署 / 采集 / 监控窗口）

**定位**：运维层（架构 §6）+ 数据层采集调度。实时监控 / 日志 / 告警 / 权限审计 / 备份恢复 / 每日采集链 / 服务器。AUTH-1 门禁、kill switch 运维端、告警分级路由、raw.db 采集均归此窗口。

**当前状态**：
| 项 | 状态 |
|---|---|
| Wave6 O1–O4（运维层） | ✅ 全终态（O4② 两项联动已实装，DN ③复核通过） |
| AUTH-1（登录门禁） | ✅ 已上线（`CS_MARKET_PASSWORD` 已配） |
| D7 raw.db | ✅ 接每日采集链（DO，18:00 起累积） |
| DJ③ D4 rebuild | ✅ 已闭环（④运维 18:22 确认，DL+DN，无需补跑） |
| S3 钉钉真实送达 | ✅ ③复核闭环（ED）：代码侧 CS 前缀已生效（④研发 98b387f：route_alert `CS【{tag}】` 前缀 + intention_card 首行 `【CS 模拟盘意向单】`）+ ④实发 push_intention 证据链闭合，310000 链路风险消除 |
| O1 回报超时两模式 | ⏳ 代码层已生效、解禁后出单观察窗口待模拟盘实际出单后自然打开（后续采集收尾日志持续跟踪） |
| raw.db 复查 | ✅ 三表正常累积（order_book/trade 各 202 / survive 18,720 行 180 天） |
| 数据健康 / collect_gate | ✅ **已闭环**：补采完成（快照 08-27 恢复 1468 行）、健康检查转 pass（FAIL 清零）、paper 冻结已人工解禁（O3 台账 20:07:52 留痕），阈值未动 |
| 备份缺口 | ✅ 首备完成闭环（④运维 18:45 执行；O4② 备份新鲜度 PASS，market_20260827_184510.db） |

**接替动作**：DJ③ D4 rebuild 已闭环（无需补跑）；**S3 钉钉③复核闭环（ED，代码侧 CS 前缀已生效+④实发证据链闭合）**；**O1 出单观察窗口待模拟盘实际出单自然打开**；**数据健康 collect_gate 已闭环（补采+health pass+paper 人工解禁 O3 20:07:52，阈值未动）**；**备份缺口首备闭环**；**decision-log 编号撞车已改号闭环（S3 关键词保证条目 ## EB→## EE）**；每日 raw.db 复查。

---

## 跨窗口接替总览（DW 定稿）

```
② R5 ✅②执行(DY)+③审计通过(DZ,无增量证伪) ──▶ R1–R5 全收口
④运维 S3 钉钉 ✅验证通过 ──▶ ④研发补 CS 前缀(98b387f) ──▶ ③ 复核闭环(ED)
④研发 立 Wave4(E1–E4) ──冒烟 147/1/0──▶ ③ 审计 EB 收口 ──▶ ④研发修 E1+E4(EC,405 真通过) ──▶ ③ ED 复核闭环（E1–E4 全通过）
④运维 DJ③ D4 rebuild ✅已闭环（无需补跑）
④运维 ✅ collect_gate 补采闭环+解禁完成（O3 20:07:52，阈值未动）+ 备份首备闭环
③ R3(DX)/R5(DZ)/EB(Wave4)/ED(S3+EC 修复) 审计均通过；**S3 ✅③复核闭环(ED)**
① PM 持续治理、落 decision-log、对照验收
```

**关键裁决（新窗口必读）**：多策略形态不成立、单引擎（融合决策）维持为架构终态；R3/R5 仅筛查层，不改引擎、不立落地卡。W7-3 多策略挂账已关闭。

**提交状态**：工作树改动已拆分（治理文档 / Wave6 代码 / 研究产物），**研发认领 commit 并 push 闭环（remote main = 2f972b2 = 本地 HEAD，PM 20:30 经 `git ls-remote` 核验一致）**；本地 `git status` 显示 `main...origin/main [gone]` 系沙箱 git 引用回读怪癖、非真实分叉（远端确含该 commit）；5 个 ops 运行时文件 `data/*.jsonl|json` 按研发意不入库、仍为 untracked（非遗漏）。
