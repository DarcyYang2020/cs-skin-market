# 角色定位 + 现状接替简报（2026-08-27 18:00，PM 编制）

> 用途：开新窗口接替工作。每个角色 = 定位（架构依据）+ 当前状态（截至 18:00）+ 接替动作。
> 权威事实源：`cs-quant-architecture.md`（六层 v1.0 定稿）、`decision-log.md`（DC→DW）、`iteration-roadmap.md`（v82 批次）。
> 全局红线（所有窗口共守）：①`oos_zone` 守院（验证段 ≥2025-08-10 / B 通道 ~2027-04-25 为禁区，探索只许 fit 段）②组合/权重**禁优化器**（等权/风险平价/规则）③**预注册判据先行**，跑前定死，不认自述、不照镜子调阈值（C2 教训）④③审计独立只读核验，不替改到通过 ⑤研究只出评估卡/注册表，**不立落地卡、不碰引擎**；旧路径 C1–C5（374 有偏样本）已整体作废。

> **✅ 更正（2026-08-27 18:22，PM 据④运维确认更新）**：原「唯一挂账 DJ③ D4 rebuild_derived 待补跑」**已清零**。④运维确认 DJ③ 已按 decision-log **DL(02:05)+DN(02:10)** 闭环——provenance 3 条留痕三跑幂等 PASS、`replay_cycle_win.db` 实测 items 405 / price_history 259,222 / market_index 1,015 且 integrity=ok、恢复演练台账 02:05:35 三库一致；**O4② 同判终态**。简报 DW 段「待补跑」系状态滞后，以 decision-log 为准，DJ③ 从挂账移除、无需二次补跑。剩余开放项见各窗口「接替动作」。

---

## ① PM（产品 / 项目经理）

**定位**：治理中枢。定"做什么、怎么算通过"——立卡三件套（目标 + 预注册判据 + 验收标准）、排优先级、按③审计结果对照验收、落 decision-log。红线：**不写代码、不改参数、不跑回测**。

**当前状态**：
- 治理链全闭环：DC（架构登记）/ DD（R1 判据冻结）/ DE（R1 组9 升主评）/ DI+DJ（Wave1 审计+D1 口径拍板 yyyp>buff>c5）/ DM（DJ 双头编号归口）/ DG+DF+DN（Wave6 落地+③审计+O4② 复核通过）/ DR（R3/R5 判据冻结）/ DT+DU（Wave3 审计+S1 账号等三项登记）/ DV（R3 结论）/ DW（角色状态整理+新窗口接替定稿）。
- 挂账：**治理层无**（DJ③ D4 rebuild 经④运维 18:22 确认已按 DL+DN 闭环，从挂账移除；见文首更正）。⚠️ **运维层待决**：①collect_gate 急停——**根因已厘清（④运维 18:45/18:50）= 08-24 快照真实缺数（1050 vs 基线 1468/−28%，采集中断提前终止，非季节别报）**，**PM 授权补采**（`run_daily_collect.py --force-weekly`）；补采后 health FAIL 解除即消除急停前提，**paper 冻结待 health 恢复后手动解除（kill switch 重置，O2 纪律），由 PM 定**；不调阈（1400 阈值与基线 1468 匹配合理）；②S3 ③依法挂起（送达链路缺关键词保障，310000 链路风险）：待④研发补 send() 关键词保证后③复核闭环。**已决**：备份缺口——老板拍板首备，**④运维 18:45 已执行闭环（O4② 备份新鲜度 PASS）**。

**接替动作**：持续治理；收口③审计（R3 审计，O4② 已随 DJ③ 闭环无需再触发）；研发认领 commit 后整体 review。

---

## ② 研究（算法研究窗口）

**定位**：研究层（架构 §2）。因子挖掘 / 特征工程 / 策略研究 / 模型训练。**基调**：符合 CS 品类特点、不照搬股市量化；方法论最成熟（预注册→族开回放→四关→③审计）。红线：**只做研究，不落地生产代码、不碰 `pipeline/` `webapp/` `tests/` 生产改动**。

**当前状态**：
| 卡 | 状态 | 关键结论 |
|---|---|---|
| R1 因子评估 | ✅ 完成（DD 冻结、DV 复核） | 21 因子评估卡，**无新候选**；供给 sc7/sc30/s7_ratio 条件 IC 唯一正信号；oos_zone 守院 |
| R2 因子注册表 | ✅ 落地（DP、DQ ③复核通过） | 21 因子：候选 3 / 证伪 15 / 存档 3（`data/factor_registry.json`） |
| R3 策略隔离评估 | ✅ 跑完（DV）+ ✅ ③审计通过（DX） | 6 族全部证伪、零漂移、无新族注入 → **多策略形态不成立，单引擎（融合决策）维持**（DX 四关独立复算全一致，对 G1 口径放宽与 G4 seed 选择均稳健） |
| R4 挖掘流程 | ✅ 完成 | 启动清单交付 |
| R5 内生情绪 v0 | ✅ ②执行完成（DY）+ ✅ ③审计通过（DZ，verdict 无增量证伪成立） | v0 固定权重 + 增量 IC 硬判据 + 仅加分过滤 |

**接替动作**：**R5 ②已执行完成（DY）+ ③审计通过（DZ，verdict 无增量证伪成立，增量 IC fit +0.0026 / val 复验 −0.0055 独立复算一致）**；**R1–R5 研究主线全收口 → Wave4（E1–E4）解冻，④研发可立卡**。

---

## ③ 审计（独立审计窗口）

**定位**：守门员。对所有落地/研究产物做**独立只读核验**（SQL 直查 / 读 JSON 产物 / 亲自重跑冒烟），结论全部由原始产物支撑，不认自述、不替②调参、不"帮忙改到通过"。覆盖：Wave1（DI）、Wave6（DG/DF/DN）、R2 registry（DQ）、Wave3（DT）、R3（DX 通过）。

**当前状态**：DI / D4 / D6 / O4②（DN）/ DF / DG / DQ(R2) / DT(Wave3) / **DZ(R5)** **全闭环**，独立重跑冒烟 142–144/0/0。
- 待办：**S3 ③依法挂起**（送达链路存在已知 310000 链路风险——send() 不保证含钉钉安全关键词「CS」，漏加即 310000 失败/告警丢失；errcode 校验已落地故非假成功，但链路无保障）。**待④研发补 send() 关键词保证（配置驱动断言/注入）后③才复核闭环**。（根因定位：notify_alert.py:74 route_alert 标题仅加【交易】标签、intention_card 格式缺 CS 前缀，④验证成功的是 monitor_mode 含 CS 格式而非 S3 intention_card；闭环选项：a) 配置侧零代码=钉钉机器人关键词加「意向单」；b) 代码侧=intention_card 标题加 CS 前缀）。🟡 非阻断登记（移交 PM 知悉，不影响结论）：**R5(DZ) 2 项**——①滚动同号月口径（②只统计 market 上下文可算日期 16 月/0.9375，审计全量 21 月/0.5238，已复现为口径选择，verdict 不受影响）；②产物 verdict_note 文案把「滚动已过」写成「未过」（仅文案，判定逻辑正确）。**R3(DX) 2 项**——①G2 双向门槛（回撤改善>10pp 亦不过，但总收益差>1000pp verdict 不受影响）；②冒烟 2 联网 API 用例因外部源限流失败（SKIP_NET=1 复跑 138/0/0，待限流解除复核闭环）。

**接替动作**：R3 审计已通过（DX）；**R5 审计已通过（DZ，verdict 无增量证伪成立）**；**S3 ③依法挂起**（送达链路缺关键词保障，待④研发补 send() 保证后③复核闭环）；O4② 已随 DJ③ 闭环，无需待补跑。

---

## ④ 研发（工程实现窗口）

**定位**：执行层（架构 §4）的代码实现方。负责数据地基 / 模拟盘 / 评估层 / 决策预留的工程落地，产出跑通 `tests/test_smoke.py` 0 failed 后移交③审计。**不碰治理决策、不改研究判据**。

**当前状态**：
| 批次 | 状态 |
|---|---|
| Wave1 D1–D7（数据地基） | ✅ ③审计通过（DI，四项登记闭环：D1 口径/D3 补用例/D4 rebuild 三跑幂等/D6 oos_guard 已接 R1） |
| Wave3 S1–S3（模拟盘） | ✅ ③审计通过（DT）；DU 三项：S1 账号默认不开 / S3 钉钉✅验证通过但**③依法挂起**（缺关键词保障，待④补 send() 保证后复核闭环）/ O1 超时两模式待运行观察 |
| Wave4 E1–E4（评估层） | 🟢 可立卡（R1–R5 全收口，R5 ③审计 DZ 通过） |
| Wave5 X1（策略抽象层接口）/ X2（流动性守卫） | ⏳ 挂账（X2 等盘口数据 3–6 月） |
| commit | 研发认领（工作树含 Wave6 代码 + 治理文档 + DJ(1970)→DM 改名 + R1–R5 预注册/审计产物 + `data/_exp_*.json`） |

**接替动作**：认领全工作树 commit（`PATH=py311` 跑 pre-commit hook）；**R1–R5 全收口 → 立 Wave4（E1–E4），跑通冒烟 0 failed 移交③审计**；Wave5 按卡推进。

---

## ④ 运维（部署 / 采集 / 监控窗口）

**定位**：运维层（架构 §6）+ 数据层采集调度。实时监控 / 日志 / 告警 / 权限审计 / 备份恢复 / 每日采集链 / 服务器。AUTH-1 门禁、kill switch 运维端、告警分级路由、raw.db 采集均归此窗口。

**当前状态**：
| 项 | 状态 |
|---|---|
| Wave6 O1–O4（运维层） | ✅ 全终态（O4② 两项联动已实装，DN ③复核通过） |
| AUTH-1（登录门禁） | ✅ 已上线（`CS_MARKET_PASSWORD` 已配） |
| D7 raw.db | ✅ 接每日采集链（DO，18:00 起累积） |
| DJ③ D4 rebuild | ✅ 已闭环（④运维 18:22 确认，DL+DN，无需补跑） |
| S3 钉钉真实送达 | ⏳ ③依法挂起（④运维验证「含CS实发成功」通过，但 send() 链路缺关键词保障、310000 链路风险待消除）；**待④研发补 send() 关键词保证后③复核闭环** |
| O1 回报超时两模式 | ⏳ 代码层已生效、模拟盘未出单、观察窗口未开（继续挂账） |
| raw.db 复查 | ✅ 三表正常累积（order_book/trade 各 202 / survive 18,720 行 180 天） |
| 数据健康 / collect_gate | ⚠️ 根因厘清（④运维 18:50）：08-24 快照 1050<1400 属**真实缺数**（非周一季节性别报）→ **PM 授权补采**（`run_daily_collect.py --force-weekly` 或 `collect_market_snapshot`）；补采后 health FAIL 解除即消除急停前提；**paper 冻结待 health 恢复后手动解除（kill switch 重置，O2 纪律），由 PM 定**，解禁前不出单 |
| 备份缺口 | ✅ 首备完成闭环（④运维 18:45 执行；O4② 备份新鲜度 PASS，market_20260827_184510.db） |

**接替动作**：DJ③ D4 rebuild 已闭环（无需补跑）；**S3 钉钉验证通过但③依法挂起（缺关键词保障，待④研发补 send() 保证后③复核闭环）**；O1 运行观察继续挂账（受 collect_gate 冻结叠加，出单观察更后移）；**数据健康：根因=真实缺数 → PM 授权补采 + 解禁待 health 恢复手动解除（不调阈）**；**备份缺口老板拍板首备（执行）**；每日 raw.db 复查。

---

## 跨窗口接替总览（DW 定稿）

```
② R5 ✅②执行(DY)+③审计通过(DZ,无增量证伪) ──▶ R1–R5 全收口
④运维 S3 钉钉 ✅验证通过 ──▶ ③ 依法挂起（待④补 send() 关键词保障后复核闭环）
④研发 认领 commit + 立 Wave4(E1–E4) ──冒烟──▶ ③ 审计
④运维 DJ③ D4 rebuild ✅已闭环（无需补跑）
④运维 ⚠️ collect_gate 真实缺数→PM授权补采+解禁待health恢复手动解除（不调阈）+ 备份缺口✅首备闭环
③ R3(DX)/R5(DZ) 审计均通过；**S3 依法挂起待④补关键词处理**
① PM 持续治理、落 decision-log、对照验收
```

**关键裁决（新窗口必读）**：多策略形态不成立、单引擎（融合决策）维持为架构终态；R3/R5 仅筛查层，不改引擎、不立落地卡。W7-3 多策略挂账已关闭。

**提交状态**：工作树改动已拆分（治理文档 / Wave6 代码 / 研究产物），**研发认领 commit，未 push**；PM 不擅提交。
