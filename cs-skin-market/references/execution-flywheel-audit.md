# 执行飞轮诊断（2026-08-15，只读，零代码改动）

## 判定

**飞轮在转但慢。**

## 三个问题的原始数据

### 1. executions 当前 8 条 action 分布

| action | 条数 |
|---|---:|
| buy（买入） | 0 |
| add（补仓） | 0 |
| reduce（减仓） | 7 |
| sell（清仓/止损） | 1 |
| hold（观察） | 0 |

### 2. executions 结算状态

- 已到期结算：**0 条**。
- `pnl_14` / `pnl_30`：8 条全部为 `NULL`。
- 未结算原因：**未到期，不是卡住**。
  - 结算口径为 `advice_date + 14 / 30 个自然日`。
  - 最早 `advice_date=2026-08-07`，14 日结算日 `2026-08-21`、30 日结算日 `2026-09-06`。
  - 今天 `2026-08-15`，因此 8 条均未到最早结算日。

### 3. signal_tracking buy 状态

- 当前实际有 **2 条 buy**，不是任务书中所述的 1 条：
  - `id=2`：`AK-47 | 轨道 Mk01 (崭新出厂)`，`signal_date=2026-08-09`，engine `v2-I13`；信号日后仅有 **6 个后续 price_history 点**，未满回填所需 14 点 → `fwd14/fwd30` 均为 `NULL`，**未到期**。
  - `id=3`：`MP9 | 绿色格纹 (崭新出厂)`，`signal_date=2026-08-14`，engine `v2-T4`；检查时信号日后 **0 个后续 price_history 点**（该品最新价仍为 2026-08-14）→ `fwd14/fwd30` 均为 `NULL`，**未到期**。
- 两条均未回填；原因 = 后验价格序列不足，不是回填任务卡住。

## 佐证

- `data/signal_tracking_reconcile.jsonl` 最近记录（2026-08-12~14）均为 `missing=[] / ok=true`，未发现生产 buy 信号漏记。
- executions 的 `7 reduce + 1 sell` 与近期止损评估建议一致，属于弱市下的正常减仓/清仓，不是“该记没记”。
- 未发现“记录后结算卡住”或“生产 buy 未落 signal_tracking”的非市场原因断点。

## 结论

飞轮在转但慢：弱市无 buy 执行导致记录自然偏少；已产生的 2 条生产 buy 均已正确落库并等待足够 `price_history` 后回填；executions 无到期未结算异常。暂不需要按“真断了”路径追加 P3 录入修复。

## 待确认

请确认判定。若确认“在转但慢”，再按任务书 §2 执行 E1（飞轮健康度仪表）；P2 可选。