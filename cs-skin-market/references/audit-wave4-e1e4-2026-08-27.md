# ③独立审计 · Wave4 评估层 E1–E4 复核（2026-08-27）

**审计对象**：④研发 Wave4 落地（decision-log EA + `references/run_quality_gate.py` + `data/_exp_quality_gate_2026-08-27.json` + E2 费率校准 `data/_exp_fee_calibration_2026-08-27.json` / `data/_exp_v2t13_fee_cal_2026-08-27.json` + `webapp/evaluate_service.py` + `webapp/templates/evaluate.html`）
**审计员**：窗口③（独立审计 / 刹车）
**红线执行**：只认 roadmap v82 Wave4 卡判据/验收 + 产物/代码事实；EA 自述仅对照。独立复核：ADF/KPSS 复现、SQL 直查比对基准、压力窗口市场走势核验、E2 等价性、evaluate_payload 实际运行、独立重跑冒烟。

---

## 一、E2 费率校准等价性（通过）

| 项 | 核验 |
|---|---|
| 费率 config 化 | `config.BACKTEST_FEES={buy_pct:0.0, sell_pct:1.0}` + `backtest_roundtrip_cost()`=1.0%，与 S2 PAPER_FEES 一致 ✅ |
| **费率不影响信号判定** | 回放信号在 T 日判定（只用 T 日及以前信息），net14/net30 是事后 14/30 日收益 → 判定不依赖 net ✅；信号集合 376 不变（差异表 note 明示）✅ |
| **重算=重跑数学等价** | net=fwd−roundtrip：old 2.0→new 1.0 平移 1.0pp，差异表 delta_avg14 全体 +1.00pp 一致 ✅；win 变化仅因阈值附近符号翻转（panic +1.3pp / deep +2.1pp / accumulate +4.0pp）✅；质量门⑤ 376 信号 net14=fwd14−1.0 mismatch=0 ✅ |
| 期望统计同步 | sync_expectancy_config 第三基线 FEE-CAL → ITEM_EXPECTANCY_STATS_FEE_CAL + BASELINE_LEDGER 注册，不动冻结基线 ✅ |
| 滑点 | 随 D2 盘口积累后补（挂账，非缺口）✅ |

## 二、E1 质量门 5 项（有条件通过：①②④⑤真通过 + ③结论通过但实现缺陷须修）

### ① 平稳性（通过，独立复现一致）
- ADF/KPSS 自实现与标准定义一致：ADF=常数+趋势+滞后差分 τ（Dickey-Fuller），KPSS=LM 统计量 + Newey-West 核（l=4·(n/100)^0.25，Bartlett 权重）+ 标准临界值（0.347/0.463/0.739）✅
- **独立复现 13/13 序列 stat 完全一致（mismatch=0）**；ADF stat −28.7~−42.1 远超 1% 临界（−4.15），KPSS 最弱 0.637<0.739 → 全平稳 ✅
- 口径说明（登记）：ADF 临界值固定 n≈50 表，对 n≈1100 序列偏保守（实际应 ≈−3.96~−4.04），stat 远超临界无影响；验证对象=价格/指数日收益（修正了初版"信号日截面值"误判），与架构 §5.1① 防伪回归目的一致 ✅

### ② 特征无泄露（通过，登记观察）
- 源码扫描 3 模式 0 findings + 9 特征定义全 OK。实质=静态声明（滚动词典人工判定）+ 有限模式扫描，非自动验证每个特征 T 日窗口——可接受（登记观察，防漏检依赖人审）

### ③ 幸存者偏差（**实现缺陷，结论正确但须修复**）
- **缺陷**：查询用 `WHERE active=0 OR status='淘汰'`，但 market.db items 表**无 active/status 列**（实际列：is_discontinued/discontinued_years/in_watchlist/holding…）→ `OperationalError: no such column: active` 被 except 吞掉 → 生产池基准为空（**prod_pool_size=0**）→ eliminated=空 是**假通过**（比对基准为空，无法检测淘汰品）
- **正确口径验证**：`is_discontinued=1`（或 discontinued_years>0）→ 淘汰品 0 个；生产池 405 = 回放池 405 完全同构（回放有生产无=0）→ **幸存者门结论实为通过**（无淘汰品在回放池）
- **判定**：结论碰巧正确但实现不可信——`prod_pool_size=0` 未校验、异常静默。**必修**（改列名 + 校验基准非空 + 失败不静默），修复后重跑登记，再复核闭环
- 附带：`replay_only_but_unknown` 产物只截前 30（`[:30]`），在基准空时是全部 405 品（AK-47 开头为排序所致）——修复列名后该字段恢复真实语义

### ④ 压力测试（通过，窗口核验有代表性 + 登记观察）
- 市场走势独立核验：**2025-10 回落期真实大跌**（−17.7%、peakDD −58.2%）；**流动性枯竭段**（2026-02~04，−7.9%、peakDD −19.5%，配合 in_sale 断档）；**2024-02 为"崩盘+反弹"复合段**（窗口整体 +18%、peakDD −15.4% 含崩盘谷底）——窗口选择有代表性 ✅
- 三窗口 avg14 全正（9.29/18.31/11.28）→ 通过 ✅
- 口径登记：**"窗口无信号=通过"**（引擎低位回避语义，但弱化压力门效力）；2024-02 标签"崩盘期"与实际"V 型反弹"略不符（含谷底，抄底信号 avg14 9.29 验证崩盘段价值）——非阻断

### ⑤ 成本真实化（通过）
- FEE-CAL 产物 376 信号 net14=fwd14−1.0%（config 同源），mismatch=0 ✅

## 三、E3 /evaluate 页（通过）

- 路由 `GET /evaluate`（AUTH-1）+ `GET /api/evaluate/data`；evaluate_payload() 实际运行：**三层全到位（missing=NONE）**
- 数据准确性核对：信号级族期望（恐慌 31.7/深值 19.2/吸筹 10.3）与 E2 差异表一致；策略级 win14 77.1/avg14 20.3 与 E2 ALL 一致；因子级 21 卡 status 分布（不稳定 9/弱无效 5/候选条件IC 3/候选无增量 1/条件因子 3）+ registry（证伪 15/候选 3/存档 3）与 R1/R2 一致；质量标签= E1 产物状态 ✅
- 费率口径诚实标注（组合级 B1 2% / 信号级 E2 1%）✅
- 登记观察："未过质量门不展示"纪律目前依赖前端展示逻辑（当前 E1=通过，无触发场景）

## 四、E4 绩效仪表盘（通过 + 2 处归因缺陷登记）

- 净值曲线 64 点（复用 b1v2.simulate cap0.8/hold21，策略 vs 大盘归一化）✅；月度 27 行、收益分布 7 桶、集中度 top5=23.3% 正常（<50%）✅
- **缺陷①（按品类归因失效）**：372/376 品落"其他"——前缀匹配（"步枪"/"手枪"…）不适配 CS 品名格式（"AK-47 | 火神"）→ 品类维度无区分度。建议用 weapon 字段或英文武器名映射（非阻断）
- **缺陷②（组合级 closed 逐笔归因空）**：n_trades=0——b1_risk_validation_v2.json 的 results 未存 closed 明细（metrics 不含 closed）→ 组合级 win_rate/avg_pnl 空。EA 声明"B1 closed 逐笔汇总"未实现（非阻断，页面已挂 fee_note 不误示）

## 五、冒烟（独立重跑）

- 全跑 **147 passed / 1 failed / 0 skipped**（1 failed = suggest API 429 限流，csQAQ 间歇限流环境问题，非 Wave4 回归）；**SKIP_NET=1 复跑 142 passed / 0 failed / 6 skipped**——非网络用例全过，**Wave4 新增 4 用例全 PASS** ✅
- 与 EA 声明"148/0/0"的差异 = 外部源限流（EA 跑时网络正常），登记环境因素

## 六、裁定

- **E2 通过**（费率等价性成立：net 平移 1.0pp、判定不依赖 net、376 验证 mismatch 0）
- **E3 通过**（三层数据准确、质量标签挂接、费率口径诚实标注）
- **E4 通过**（净值/月度/分布/集中度准确）+ 2 处归因缺陷登记（按品类失效、closed 逐笔空）
- **E1 有条件通过**：①②④⑤真通过；**③幸存者门实现缺陷（列名错误→基准空→假通过）须修复**——正确口径下结论实为通过（无淘汰品），修复（改 is_discontinued 列名 + 校验基准非空 + 异常不静默）后重跑登记，再复核闭环
- 不踩红线：全部只读评估层、零引擎决策改动、不 bump ENGINE_VERSION ✅；审计未替研发改代码到通过
- **登记**：4 项非阻断（E1 压力窗口/ADF 临界值口径 + E4 两处归因缺陷）+ 1 项必修（E1③幸存者门）
