# 迭代方案（Iteration Roadmap）

> **迭代闭环规则（2026-08-06 定稿）**：迭代执行 → 学习/试错 → 提炼新思路 → 映射为迭代项 →
> 更新本方案（版本 + 日期）→ 按优先级继续迭代。**若某轮实在没学到新东西，则不更新版本**（不为了更新而更新）。
> 分工：本文件 = 计划（接下来做什么）；`decision-log.md` = 历史（为什么 / 试错细节）。
> 变更纪律：每次更新先写版本变更说明，再改映射表/批次/状态。

## 版本历史
- **v58（2026-08-13，进行中）**：直连 API 数据储备排期——P0 已完成（活跃池基本面+求购历史），P1 首批已完成（存世量+系列面板+大户 Top20），P2 全市场暂缓；研究层优先，不接引擎。详见 `references/data-reserve-api-audit.md`。
- **v57（2026-08-13，计划）**：产品侧下一版迭代方案（A-9 落地后）——A-9 验收 + 采集预算回收观测、A-2 执行环轻推（买入侧闭环）、生产 buy 首条实盘观测；B 线维持回测先行/等样本。零引擎参数优先。
- **v56（2026-08-13）**：贴纸模块降级——停采停扫 + 观察桶冻结 + 保留静态研究资产；159 个非自选/持仓贴纸标记 `贴纸模块停采`，discover 移除贴纸榜，每日 W-3 跟踪调度摘除。零引擎参数。详见 decision-log「贴纸模块降级」。
- **v55（2026-08-13）**：主引擎优先落地——A1-2 deep_value sent 66-74 无主区复验不落地；主引擎四处裸 except 收窄为 logger.warning（行为等价）。产物 `data/_exp_a1_2_sent_gap.json`。详见 decision-log「主引擎 A1-2 复验 + 异常吞噬收窄」。
- **v54（2026-08-13）**：signal_tracking 唯一一行乱码修复（备份可回滚）+ 供给×求购观察层落地（`probe_supply_bid_observer.py` → `_exp_supply_bid_observer.json`）。零决策参数，正式 A2 继续等样本。
- **v53（2026-08-13）**：PANIC-ALIGN-1 阶段0完成——回放恐慌 92 条全部为价格近似情绪；真实贪婪情绪与近似情绪 60 天重叠窗口 spearman +0.118、`sent>=75` 一致率 46.7%；生产 panic 可判定样本 0。只报告不判定，不进入 A2。产物 `data/_exp_panic_alignment.json`，详见 decision-log「PANIC-ALIGN-1 阶段0」。
- **v52（2026-08-13）**：PORT-1 组合风险预算阶段0完成——317 信号只读组合模拟，vol/regime/vol_regime 三档均较静态 cap0.8 更差，置换检验 p=0.18 不显著；不进入正式 A2，维持 cap0.8。产物 `data/_exp_portfolio_risk_budget.json`，详见 decision-log「PORT-1 阶段0」。
- **v51（2026-08-13）**：量化优化六项补充立项——PORT-1 组合风险预算 / PANIC-ALIGN-1 恐慌口径一致性 / BID-1 求购因子 / STICKER-1 贴纸庄盘 / EVENT-1 事件日历 / CROSS-1 跨平台价差。全部为候选立项，零决策参数；详见 decision-log 2026-08-13 条目。
- **v50（2026-08-13）**：量化优化三项立项排期——EXIT-1 路径依赖退出（P0）/ LIQ-1 流动性成本感知仓位（P1）/ SUPPLY-CONF-1 供给收缩机制分离（P2）。仅立项与阶段 0 只读探针，不改引擎参数；正式 A2 判定窗口：EXIT-1 2026-08-24，LIQ-1/SUPPLY-CONF-1 2026-11-09~13。详见 decision-log 2026-08-13 条目。
- **v49（2026-08-10）**：四项审计落地（周期权重反转 / panic 分级仓位修复 / 概率去 z 化 / 供给降仓证伪）——评估指标体系第一性原理审计结果落地，回测先行：
  - **周期权重反转**：周期分 洗盘 2.5 > 吸筹 2.0 > 拉升 1.2（原吸筹>拉升>洗盘）；365d 回放洗盘期最优（win14 82.2%/+18.9、win30 +30.6），拉升期追高 win14 63% 最差。
  - **panic 分级仓位修复**：panic_resonance 跳过分级（保持族级 0.30），修复分级覆盖族级参数的架构 bug；基础族恢复旧分档（≥8.5→0.30 / ≥7→0.20 / ≥5→0.12）。
  - **概率去 z 化**：base_up 改由波动率 regime 主导（stable 65 / normal 55 / volatile 48 / high_volatile 42），消除与位置 40% 双计权，Z 仅保留展示口径。
  - **供给降仓证伪（未落地）**：supply_accum 0.10→0.05 组合模拟 -12.9pp，维持 0.10。
  - **回放基线变更**：365 天窗口（2025-08-10 起，保留策略下限），332 信号；等权口径与旧引擎 365d 完全一致（win14 69.9%/+15.36）。
  - **A/B**：旧→v3 wavg14 19.71→20.48 / wwin14 72.9→74.4、wavg30 22.07→23.27 / wwin30 64.0→65.8；组合 +86.03%/-13.36% → +83.65%/-13.05%；实验产物 `data/_exp_*.json` 归档。
  - 验证：冒烟 84 passed / 0 failed / 6 skipped；详见 decision-log「四项审计落地」。
- **v48（2026-08-09）**：F-3.6 价格串品治理——fetch_kline_90d 加悠悠锚校验 + 全库体检修正（用户报「黑龙纹身价格不对」）：
  - **根因**：每日采集路径 `fetch_kline_90d` 无串品防护（`fetch_item_detail` 有锚校验），批量刷新偶发捕获错误 chart 直接整段覆盖落库；黑龙纹身真实悠悠锚 ¥687.5（在售 371），库里 08-01 起存的却是 1000+/在售 100 的串品数据。
  - **修复**：`fetch_kline_90d` 捕获多 chart + info/good，用悠悠锚（DOM 价 + yyyp_sell_num）`_pick_best_chart` 挑选 + 偏差校验（价格>20%/在售量>30% 判串品），可疑重试一轮，仍可疑返回空——调用方不落库保留旧数据。
  - **数据修正**：黑龙纹身覆盖修正；体检 82 品（高分榜 Top20 + 自选/持仓）另发现 7 品错价（偏差 11%~58%：弹雨 2428→1697、二号玩家 1593→1008、鬼退治 4170→6722、暴怒野兽 1108→1651、气密 323→208、目皆转睛 206→135、神祗 40→45）全部覆盖；3 品采集空保留旧数据待每日采集补齐。
  - **高分榜重扫**：buy 1→0——原 buy（弹雨）建立在错价上修正后消失；当前 Top10 全 watch（TH=37 弱市），榜首沙漠之鹰 午夜凶匪（8.0）。
  - **后续**：其余约 100 品未逐一体检（夜间限流保护），采集器修复后今日 18:00 每日采集全量覆盖自动纠正。
  - 学到的新思路：① 同源数据两条采集路径防护不对称（分析路径有锚校验、采集路径没有）是数据污染温床——串品防护必须下沉到所有写库入口；② 体检优先覆盖用户可见面（高分榜+自选持仓），全量靠每日采集自动收敛，避免夜间长跑触发限流；③ 高分榜 buy 信号要复核底层价格真实性，错价会制造假信号。
- **v47（2026-08-08）**：F-3.5 高分品模块治理——磨损/类型过滤 + 流动性闸门（用户报「渐变斑纹存世量少在售量更少却排第一 buy」「AK 精英之作战痕累累也被收录」）：
  - **池治理（非崭新出厂淘汰）**：库内非「崭新出厂」且非自选/非持仓的 7 品（战痕累累/略有磨损/久经沙场/印花/角色等）标记「活跃池淘汰:非崭新出厂」退出每日采集，写台账 type=prune；discover 池内扫描 SQL 加 `i.name LIKE '%崭新出厂%'`，自选/持仓的非 FN 品保留采集与监控但不进高分榜。
  - **流动性闸门（决策层，F-3.5）**：`compute_fusion_decision` 新增 `supply_depth`（最新在售量）参数，buy 时 `0<supply_depth<15` → 降级 watch「流动性不足·观望」（渐变斑纹在售 13，流动性总分 63 远超旧闸门 <30 所以此前漏网）；`decide_fusion_signal` 恐慌共振与后置族升级条件加 `not fd.liquidity_filtered`，堵住「闸门降级后被供给收缩族升回 buy」的绕过路径。
  - **高分榜预筛**：discover `_analyze_one` 近 7 天平均在售量 <15 → skip「流动性不足」，低流动品不进榜单。
  - 验证：冒烟 85 passed（新增 t_liquidity_depth_gate 单函数 + t_liquidity_gate_e2e 真实库渐变斑纹端到端禁 buy）；真库重扫 170 候选 / 64 通过 / 106 跳过，Top10 无渐变斑纹/战痕累累/非 FN 品，buy 仅 M4A4 弹雨（在售 18 >15 保留）。
  - 学到的新思路：① 低流动性防御只加在基础决策层会被「后置族升级」绕过——闸门必须同时拦截所有 action 升级路径，测试要端到端（真实品）而非只测单函数；② 磨损/类型约束要落到「池」与「榜」两层：自选/持仓允许任意品（用户真实持有），但高分榜严格只收崭新出厂；③ 发现高分品从搜索路径切到分析路径后，数据质量问题直接从池内暴露，治理重点从「采什么」变成「池里留什么」。
- **v46（2026-08-08）**：F-3.4 discover 改为从池内跑（用户「寻找高分品模块能不能改为从池子里跑」）——不再依赖 csQAQ 搜索 suggest（滑块验证码风险），池内活跃品 DB 新鲜 K 线复用扫描：
  - `POST /api/discover/scan-all` 新增 mode 参数（默认 pool）；`_run_discover_pool_task` 加载活跃池品（good_id>0，排除存世量过低/活跃池淘汰）→ 走 `_run_discover_task` 复用优先分析；`mode=search` 保留原全网搜索扩池路径（手动扩池用）。
  - 完成产物（latest cache + top10 历史存档 + 池维护台账）抽 `_save_discover_artifacts` 公共函数，pool/search 共用，消除尾部重复逻辑。
  - 页面文案更新：池内扫描说明 + 按钮改「🚀 扫描池内高分品」；刷新入口不变（默认 pool 模式）。
  - 验证：冒烟 83/0/6；真库扫描 181 活跃品 / 70 通过 / 111 跳过（弱市 P3 过滤+分位过高），纯 DB 约 2.5 分钟；Top10 含 2 个 buy（M4A4 渐变斑纹 / M4A4 弹雨）；台账 completed（pool_size_now=188）。
  - 学到的新思路：① 数据积累到存量后，「找机会」从采集路径切换为分析路径——池内 90 日 K 线每日在库，全池扫描是纯 DB 计算而非网络采集，速度与稳定性大幅提升；② 弱市（TH<55）下 P3 过滤天然把展示收敛为高分品，与「发现高分品」定位一致；③ 两条路径共用的尾部产物必须先抽公共函数，避免同逻辑两处维护漂移。

- **v45（2026-08-08）**：F-1.1 信号→动作统一映射（用户反馈「回调中·关注对应什么动作？筑底对应什么？批量扫描太乱」）——watch 类信号不再硬映射建仓，统一为「观望」：
  - 统一动作体系：建仓/补仓/减仓/清仓·止损/观望（exec-modal 新增「⏸️ 观望」）；`batch_scan.FUSION_TO_EXEC` 单一映射表，单品/批量扫描/弹窗三处共用。
  - 单品报告决策条新增「建议动作」徽章（回调中·关注/筑底中·观察→观望），执行按钮 data-action 统一映射（watch→hold）。
  - 批量扫描建议单元格长文案收敛为动作徽章（🟢 建仓/补仓 · 🔻 减仓 · 🔴 清仓/止损 · ⏸️ 观望 · ⛔ 禁补），观望类也明确显示；弹窗显示「信号 → 建议动作（可修改）」。
  - 验证：冒烟 77/0/6（t_report_exec_btn 断言更新 watch→hold）；模板渲染验证。
- **v44（2026-08-08）**：F-3 采集复用优先（用户「采取最优采集方案，不要重复采集」）——DB 新鲜度复用 + 失败重试一轮 + 进度落盘，全部在采集/数据/展示层，零触碰冻结参数：
  - **DB 新鲜度复用优先**：`analysis_service.db_kline_fresh`（行数>=14 + 最新日期距今<=阈值）→ 新鲜则 `item_from_db` 构造 ItemData 跳过网络采集；阈值 KLINE_FRESH_SINGLE=0（单品主动分析）/ KLINE_FRESH_BATCH=3（批量扫描）/ KLINE_FRESH_DISCOVER=3（discover）。
  - **失败重试一轮**：discover 首轮失败/error 的品轮尾统一重试（复用优先，DB 新鲜的秒过）；**进度落盘**：每品 `_persist_discover_progress`，重启可查进度，完成即删。
  - **验证**：冒烟 77/0/6（新增 t_kline_fresh 新鲜度判定、t_resolve_item_reuse mock 验证不触发网络采集）；/ /watchlist /discover-progress 渲染 200；服务器重启加载新代码。
  - 学到的新思路：① 新鲜度判定两个前提（行数下限 + 最新日期距今天数）缺一不可，避免「历史长但已过期」误复用；② 测试断言必须对齐数据源真实口径——get_item_history 按 date DESC LIMIT 90，91 行只返回 90 条；③ 超大函数整体替换前先确认函数边界与相邻依赖（误删 _render_discover_html 后已恢复）。
- **v43（2026-08-08）**：P0 第一性原理落地第一批——F-1 一键执行录入 + F-2 执行复盘对照卡（用户「开始」后实施，展示/数据层零触冻结）：
  - **F-1 一键执行录入**：exec-modal 共享化——新增 `partials/exec_modal.html`（modal 标记 + openExecModal/submitExec，参考价兜底 ref-price 保留），base.html 全站引入，watchlist 本地 modal 与 2 个函数去重；
    单品报告决策条下加「💼 按建议记录执行」按钮（data-name/action/signal/price，默认动作映射 buy/reduce/sell，avoid/watch 可改），报告/自选/批量扫描/discover 全部打通「1 次点击录入 + 同步持仓」。
  - **F-2 执行复盘对照卡**：`dashboards.execution_review` 聚合——真实口径 executions 已结算 pnl14/pnl30/合并 + 滑点（advice_price→exec_price）；纸面口径 signal_tracking net14/net30；
    新增 GET /api/executions/review（先跑自动结算再统计）；watchlist 执行记录区下方新增「📊 真实 vs 纸面」对照卡（样本/14日/30日/滑点）。
  - 验证：冒烟 75/0/6（新增 t_report_exec_btn 模板渲染 + t_exec_review 增量口径）；六页面（/ /search /watchlist /discover /replay /checkup）渲染 200 且含共享 modal；
    真库 API 闭环（插入→review n=2/滑点 -0.06%→删除恢复）；ref-price 822.5 悠悠锚口径正常。
  - 学到的新思路：① 执行录入的零摩擦 = 把报告页与执行弹窗打通——共享 modal 一次落地全站受益，避免每页复制一份；② 复盘聚合测试用「基线差值」断言，防测试污染真库导致误红；③ 浮点断言必须 round 到与产品展示同精度（avg 2 位小数）。
- **v42（2026-08-08）**：第一性原理差距分析落档 + P0-P2 落地计划（用户问「离真正好用的量化工具还差什么」，确认「可以」后落地）：
  - 产出 `references/first-principles-gap.md`：系统定位为「防守型纸面量化工具」——引擎边际价值在回撤控制
    （策略 +193.30%/-9.39% vs 池内等权 +509.75%/-54.12% vs 大盘 -4.02%，见 data/benchmark_compare.json），收益增强 alpha 未被验证；
    最大断点是「建议→真实成交→回报」飞轮未转（executions 表/API/自动结算 2026-08-04 已建，但库内仅 1 条手动记录）。
  - 落地计划（P0-P2，全不触碰冻结参数）：
    - **P0（数据飞轮，零依赖可开工）**：F-1 报告页「按建议记录执行」一键入口（复用 executions/ref-price 自动带名称/日期/信号/参考价）；
      F-2 组合仪表盘「执行复盘对照卡」（真实累计 pnl 扣 2% / 平均滑点 / 14-30 胜率 vs signal_tracking 纸面对照）；
      F-3 discover 全量重扫扩池（csQAQ 限流约 25-40 分钟）。
    - **P1（收益可信，研究层）**：F-4 成本建模进回测（价差+手续费+流动性冲击，in_sale_count 估冲击，cost_sensitivity.py 扩展）；
      F-5 组合模拟器（资金分配/相关性/再平衡，cap0.8 升级候选，待数据评估后议是否进引擎）。
    - **P2（体验前瞻）**：F-6 事件驱动提示（Major/开箱/新箱，I-12 对齐）；F-7 钉钉卡片一键执行/移动端（二次确认+幂等）。
  - 验证：纯文档层（first-principles-gap.md + 映射表 F-1~F-7 + decision-log 补录）；无代码改动，冒烟保持 73/0/6。
  - 学到的新思路：① 「执行流水」的断点不是表结构而是摩擦与可见性——建表后 1 条记录都没有，反馈闭环必须
    「录入零摩擦 + 收益对照可见」才会被使用；② 第一性分析必须核对已落地资产（P0-2 执行记录/ref-price/自动结算已存在），
    避免把已有能力当缺口重复规划；③ 从「纸面信号回测」到「真实执行净值」的桥是量化工具从分析器变成投资系统的分水岭，
    桥的起点是让录入变成一次点击。
- **v41（2026-08-08）**：每日采集提前至 18:00 + 晚间推送解耦（用户「数据采集提前」）：
  - 背景：全量采集原 21:30，用户希望采集提前；csQAQ 指数/在售量实时、K 线随时可采，无 21:30 硬依赖；提前到 18:00 当日 bar 覆盖白天主交易时段，口径损失可控。
  - 落地：`run_daily_monitor` 加 `push` 参数（默认 True）；`run_daily_collect` 收尾改 `push=False`（事件/日报保留，不推送）；新增 `run_night_push.py` 21:30 独立推送（事件按 slot 前缀幂等去重，重跑不重复，推送保持 12:00+21:30 两时段）。
  - 计划任务：`CS_Skin_DailyCollect` 21:30→18:00；新增 `CS_Skin_NightPush` 21:30；`install_tasks.ps1` 补齐全量注册（ASCII 追加，原文件 GBK 混合编码不动）。
  - 验证：冒烟 73/0/6；py_compile 全过。
  - 学到的新思路：① 采集时间与推送时间可以解耦——推送幂等 key 只认日期+slot，独立任务重跑同 slot 安全；② 改 Windows 计划任务用 `schtasks /Change` /`/Create`，脚本化注册时注意原文件编码（GBK/UTF-8 混合别强解）。
- **v40（2026-08-08）**：大盘采集 401 自动重绑 IP（用户报「获取大盘数据失败」）：
  - 根因：csQAQ 绑定 IP 校验，环境出口 IP 轮换导致间歇 401；Web 刷新路径无绑定机制，浏览器兜底在请求线程内跨 loop 不可用 → 返回 None。
  - 修复：`collector` 的 current_data / sub/kline 401 分支先 `bind_local_ip()` 重新绑定再重试一次，仍 401 才走浏览器兜底；kline 解析抽 `_parse_kline_points` 复用。
  - 验证：真实环境 bind 后恢复（指数 1586.01 / K线 996 点）；新增 `t_market_401_rebind_retry` 冒烟；73/0/6。
  - 学到的新思路：① 第三方 API 的「绑定 IP」类间歇 401 应先走官方重绑接口再兜底，浏览器兜底跨 loop 成本高且不可靠；② 报错信息应区分「可自愈」（重绑/重试）与「真失败」，先自愈再报错。
- **v39（2026-08-08）**：求购数据持久化（数据储备，为后续版本迭代验证求购因子攒样本；决策/展示零改动）：
  - T1 实测（8 品）：order_book 可得性 8/8；`bid_30d_chg` 口径不可信（8/8 极端负值背离售价）→ 禁用仅存档；`spread_avg` 有 0 脏值；`bid_count` 为估算假数据不持久化。
  - 落地：snapshots 新增 5 列（bid_highest/bid_7d_chg/bid_30d_chg/spread_pct/spread_avg），`save_item_snapshot` 统一写入；新增 `t_snapshot_bid_cols` 冒烟。
  - 验证计划：积累 ≥60 天后拆「求购配合 vs 背离」对比供给收缩事件 14/30 日胜率；决策融入需证据 + 2027-04 OOS 后议。
  - 学到的新思路：① 新因子接入前先做「可得性 + 口径一致性」实测（同源字段 30 日趋势与售价背离即判不可信）；② 数据储备与决策解耦——先攒样本、后验证据、再谈融入，避免用未验证因子污染冻结期决策。
- **v38（2026-08-08）**：买点路径达标度展示优化（用户追问「81% 是什么 / 横杠是什么」，纯展示层）：
  - 缺口文案三段式：`条件名：当前值（需 目标）` + 人话 hint（如「供给收缩：7日/30日在售量 0.97（需 ≤0.85，收缩15%+才达标）」「位置分位：45%（需 ≤30%，越低越便宜）」）。
  - 横杠可解释：score==0 返回 zero_reason——数据不足 →「数据不足，无法评估买点路径」；各路径均有硬缺口 → 列 top2 清零条件（带人话，如「7日去重（7日内已买过）」）。
  - 单品报告 81% 旁加注「该路径各条件综合达标度，非买入概率」；watchlist 0 分条目改显「暂无接近路径」。
  - 冻结合规：compute_buy_proximity 为纯展示层，族条件/评分方式零改动。
  - 验证：离线 smoke 71/0/6；函数级三场景（供给收缩 0.97 白话缺口 / 7日去重清零原因 / 数据不足原因）。
  - 学到的新思路：① 几何平均一票否决是横杠主因（7日去重=0 可同时清零 5 族），清零原因必须显式化；② 展示数字要带「度量语义（达标度）＋阈值语义（需 ≤0.85）＋行动语义（收缩 15%+ 才达标）」三段，否则使用者无法行动。
- **v37（2026-08-08）**：系统冗余清理全量落地（用户「都做」，先删 dashboard 信号中心再批量清理）：
  - dashboard 移除信号中心模块（与自选页/监控功能重复，用户反馈）；`/api/watchlist/scan-history` 保留（watchlist 仍在用）。
  - A 类：6 个零引用回测脚本归档 references/scripts-archive/（当前回测统一走 refit_pipeline.py）；删除 CS_DB_Backup 计划任务（备份由采集收尾负责，避免每日两次）；事件类型标签单一事实源（monitor.html 由后端注入 _TYPE_LABEL）；backtest_results 死数据清空+标注废弃。
  - B 类：大盘指数统计抽公共函数 market_context.market_index_stats（analysis_service 引擎路径与 monitor 监控路径复用同源计算，情绪口径各自保留）；main.py 抽 _load_j2() 共用 j2_channel_status.json 加载（dashboard 徽章 + checkup 体检）。
  - C 类：清理 settings 中 6 个已删除品目 th_* 缓存残留；PROJECT_STRUCTURE.md 标注文件结构唯一事实源，AGENTS.md 文件树加注 + 更新归档脚本说明。
  - 保留项（评估后不动）：watchlist 监控提醒卡（/monitor 子集摘要，自选页快速看）、K 线三路径（全量/轻量/按需互补）、dashboard 信号密度 vs checkup C 通道（热度 vs 胜率不同维度）、market_snapshot/monitor_rank_snapshot 周度积累（研究价值）。
  - 验证：离线 smoke 71/0/6（移除 2 个随脚本归档的期望口径测试，口径已由 t_expectancy_sync 覆盖）；pre-commit 全量 77/0/0；编码健康 PASS；/ /monitor /checkup 渲染 200（type_label 后端注入生效）。
  - 学到的新思路：① 归档脚本前必须 rg 全仓含 tests/ 的引用（此前遗漏导致冒烟 ModuleNotFoundError）；② 并行工作区下提交要「备份-分离-恢复」，绝不混入他人未提交改动；③ 口径类测试随工具归档后，其价值要能由更高层校验（t_expectancy_sync）承接。
- **v36（2026-08-08）**：监控推送改纯文字自包含——移除内网 URL，danger/warn 明细直接列出（用户反馈手机无法访问 127.0.0.1）：
  - 背景：推送正文含「信息 N 条（详见 http://127.0.0.1:8000/monitor）」，手机端打不开本机地址；要求信息直接写在推送里。
  - 落地：`_build_push_text` 重构——🔴 危险/🟡 提醒明细直接列出（各截断 PUSH_DETAIL_MAX，超出计数）；🔵 信息按类型计数（如「大盘状态 2 条」）；彻底移除内网 URL 行。
  - 测试：t_monitor_push 新增回归断言「推送文本不得含 127.0.0.1 / :8000」。
  - 验证：离线 smoke 73/0/6；真库实测推送（3 危险明细 + 24 提醒明细 + 2 信息计数）钉钉 HTTP 200 / errcode 0。
  - 学到的新思路：① 面向手机的提醒必须自包含，内网 URL 在移动端不可达等于无效信息；② 事件明细直接文字化比「详见网页」对提醒类消费更有价值；③ 推送格式要有防内网地址回归断言。
- **v35（2026-08-08）**：监控日报双时段推送——午间 12:00 + 晚间 21:30（用户建议，纯提醒层）：
  - 背景：用户建议改为两次推送（中午 12:00 + 晚上 21:30）；核心价值：午间看盘中变化、晚间看收盘定论，事件与推送均按 slot 独立幂等。
  - slot 机制：`run_daily_monitor(date, slot)`，slot=noon/night；事件 dedup_key 统一加 `slot::` 前缀（同日两套事件快照各自幂等）；
    `push_daily` 幂等 key 改 `monitor_push_{date}_{slot}`；钉钉标题带时段（午间/晚间）；`db.list_monitor_events` 解析前缀返回 slot，/monitor 页面按 日期+时段 分组。
  - 午间入口：新增 `run_daily_monitor.py`——重绑 csQAQ IP → 大盘指数刷新（HTTP 轻量）→ 自选/持仓品 90 日 K 线轻量刷新（Playwright，实测 20 品约 5 分钟）→ `run_daily_monitor(slot="noon")`，
    保证午间推送有当日盘中数据增量而非昨晚快照；晚间仍由 run_daily_collect 收尾（slot=night）。
  - 调度：`install_tasks.ps1` 新增 `CS_Skin_NoonMonitor` 12:00（已注册，下次 2026-08-08 12:00）；晚间 21:30 采集收尾不变。
  - 顺带修复 M2 遗留 bug：`push_daily` 写幂等 key 后未 commit（close 回滚 → 同日重跑会重复推送），加 `conn.commit()` + 回归用例。
  - 验证：离线 smoke 73/0/6（新增 M3 双时段 slot 用例 + 幂等持久化用例）；真库午间链路跑通（IP 绑定 / 大盘 1582.15 / 自选品 K 线 20 品 / 13 事件 / 钉钉实际收到午间日报）；编码健康 PASS。
  - 学到的新思路：① settings 类幂等标记必须 commit，否则 close 回滚变成「永远不幂等」的假幂等；② 多时段推送必须带数据增量（轻量 K 线刷新），否则午间推的是昨晚快照；③ 同一日期多时段事件用 dedup_key 前缀区分，天然满足「各自幂等 + 页面分组」双需求。
- **v34（2026-08-08）**：M2 钉钉推送配置验证 + send() errcode 加固（用户提供 webhook 后收尾）：
  - 背景：用户配置好钉钉机器人并给出 webhook（安全设置自定义关键词「监控」）；写入 .env 后试推返回 310000 关键词不匹配，
    排查发现：① 关键词本身没错，是 PowerShell 管道喂 Python 时中文变 ? 导致实际推送乱码；② 原 send() 只看 HTTP 200 不校验钉钉业务 errcode，存在假成功。
  - 落地：根 .env 写入 NOTIFY_WEBHOOK_URL（.gitignore 已忽略）；`notify_alert.send()` 解析响应 JSON，errcode != 0 抛 RuntimeError
    （310000 等业务失败不再被当作成功，notify_alert._push / monitor.push_daily 的 except 自动转失败原因）；tests 新增 t_notify_send_errcode（mock urlopen 双分支）。
  - 验证：base64 无编码损失方式手动推送 errcode:0；run_daily_monitor 真库跑通（25 品分析 / 15 事件幂等 saved=0 / pushed:true，钉钉实际收到日报）；
    离线 smoke 71/0/6（新增 1 用例）；编码健康 PASS。
  - 学到的新思路：① 钉钉 HTTP 200 不等于成功，推送必须校验业务 errcode，否则告警静默丢失；② Windows PowerShell 管道喂 Python 会破坏中文，
    凡涉及中文的推送验证/写文件要走 base64 或 UTF-8 文件通道；③ 推送链路验证要端到端（webhook → errcode → 群内收到），只验证 HTTP 状态是假验证。
- **v33（2026-08-08）**：M2 监控钉钉推送（用户确认 M1 后实施，纯提醒层）：
  - 复用 notify_alert.py（.env NOTIFY_WEBHOOK_URL）；run_daily_monitor 收尾自动推送：标题含日期+大盘状态+🚨危险数/🟡提醒数，正文 danger 明细全列（截断 10 条）+ warn/info 计数 + /monitor 链接。
  - 幂等：settings 记 `monitor_push_{date}=1`，同一天重跑不重复推送；未配置 webhook / 推送失败均不中断采集（summary.pushed 返回原因）。
  - 验证：离线 smoke 70/0/6（新增 M2 正文组装 + 无 webhook 跳过用例）；真库跑通（saved=0 幂等、pushed=no_webhook）。
  - 学到的新思路：① 推送幂等用 settings 日期 key，重跑采集不重复打扰；② 提醒类推送复用健康告警通道（notify_alert），不另起基础设施；③ 未配置 webhook 时静默跳过并返回原因，采集主流程零耦合。
- **v32（2026-08-08）**：M1 监控模式落地（用户确认方案后实施，纯提醒层，冻结纪律内）：
  - 目标：从「打开系统看报告」变为「每日自动盯盘」——`run_daily_collect` 收尾自动生成自选品异动事件 + 日报，Web `/monitor` 页面展示近 7 天归档。
  - 事件规则（8 类，阈值人工确认不回测拟合）：买点接近（proximity≥60 且非 buy）/ 破位止损（现价≤成本-25%）/ 决策翻转（watch|avoid↔buy）/ 供给突变（在售量 7 日 -20%/+30%）/ 价格异动（单日 |涨跌|≥8%）/ 大盘状态切换（六态跨日变化）/ 持仓到期（executions 14/30 日复盘到期）/ 新 buy 信号（signal_tracking 当日新增）。
  - 落地：`pipeline/monitor.py`（大盘上下文纯 DB 计算不拉网络 + 自选品只读 run_item_analysis 不落库 + 事件生成 + `data/monitor_daily.md` 日报）；`db.py` 新表 `monitor_events`（dedup_key 幂等防重复）；`run_daily_collect.py` 收尾接入；`webapp/main.py` `/monitor` + `/api/monitor/events`；`monitor.html` 按日期分组卡片（危险/提醒/信息三级）。
  - 实测：25 自选品分析 2.2s，首跑生成 16 事件（2 破位止损 + 12 买点接近 + 2 价格异动 + 大盘状态），重跑幂等（saved=0）。
  - 验证：离线 smoke 69/0/6（新增 M1 事件生成 + 空库守卫 2 用例）；编码健康 0 硬问题。
  - 学到的新思路：① 提醒层规则不引入回测（阈值人工确认），避免给冻结期叠加过拟合面；② 监控分析走 DB 最新 K 线只读跑引擎（不落库），复用日采数据零额外采集成本；③ 事件表用 dedup_key（date|item_id|type）幂等，当日重跑不重复、跨日自动归档。
- **v31（2026-08-08）**：每日采集优化——全市场快照/大户集中度降为每周一（用户「检查每日采集内容，有无没必要采集的数据」）：
  - 审计结论：每日 10 环节中，**大户集中度**（monitor/rank 每品 Top50，~8-10 分钟/天 Playwright）与**全市场快照**（get_page_list 25 页，~1 分钟/天）引擎/决策/展示均不消费（whale 检测只用价格+供给；两表仅数据进度卡覆盖天数 + 健康检查计数）；贪婪历史（macro_history）写穿透已在实时调用时落库，每日显式兜底 1 个 HTTP 保留；K 线全量刷新（价格+在售量=引擎唯一数据源）保持每日无条件。
  - 落地：run_daily_collect.py 新增 `is_weekly_collect_day`（BJ 周一，`CS_WEEKLY_ALWAYS=1` / `--force-weekly` 可补采），快照+大户仅周一执行；run_data_health.py 两检查 age 阈值 4→8 天；collector_monitor/collector_snapshot 保留为手动工具；进度卡字段保留（test 契约不动）。
  - 收益：每天省 ~9-11 分钟 Playwright 负载（大户逐品最重），数据积累能力保留（每周一份）。
  - 学到的新思路：① 采集审计看「引擎/决策/展示」三级消费链，只被健康检查计数的数据可降频不可盲停；② 降频必须同步放宽健康检查 age 阈值，否则健康度恒 FAIL 变噪声告警；③ 有明确研究价值但未接入的数据（大户集中度）用「周度积累」保底，优于完全停采。
- **v32（2026-08-08）**：Discover 高分品报告串品修复（采集层+展示层，决策零改动）——用户「高分品里 沙漠之鹰 | 钴蓝禁锢 (崭新出厂) 报告错误，应该是拿错品了」：
  - **根因**：discover 扫描直连消费 `fetch_item_detail` 的 K 线，而采集偶发捕获到非悠悠 chart——13:53 该品捕获到 **Steam chart**（价 1187.16 / 在售 97，与 info/good 的 steam_sell_price / steam_sell_num 完全一致；悠悠锚 824/577），报告按错误 K 线生成（当前价 1187.16、90 日最低 1175、MA30 1261）；实测另偶发捕获 **Buff chart**（在售 336 vs 悠悠 577，偏差 42%）。discover 路径缺少单品分析路径的 `anchor_override` / `kline_price_sane` 锚价防护（analyze_fresh 与批量扫描均有），错误数据直接进入评分/供给/估值/报告（列表价走 DOM 悠悠锚正确，报告价走错误 chart）。
  - **修复**：
    - `collector_csqaq.py`：captured 改多 chart 列表捕获 + `_pick_best_chart`（悠悠 DOM 价 + yyyp_sell_num 双锚打分）；新增 `_kline_matches_anchor`（价偏差>20% / 在售偏差>30% 判串品），`fetch_item_detail` 不符自愈重试 3 次，仍不符清空 K 线交由调用方回退 DB（悠悠口径）；`_wait_chart` 参数化 key 兼容既有调用方。
    - `webapp/main.py` `_run_discover_task`：新增串品防护——K 线最新价/在售 vs 悠悠锚双校验，不合格重取一次，仍不合格回退 DB K 线，再不行跳过该品（不产出错误报告）。
    - 数据修复：钴蓝禁锢 analysis_results + snapshot 用正确数据（823/579）重新生成，discover 报告页已恢复（恐慌退潮 73%，不再显示 Steam 口径 1187/97）。
  - **验证**：fetch_item_detail(541) 连续 5 次全部返回悠悠口径（823/579）；冒烟 73/0/6；py_compile 通过；服务器重启后全量 discover 重扫确认列表价与报告口径一致。
  - **学到的新思路**：① 「拦截页面自带请求」采集天然不可靠（页面 localStorage 固定 chart 设置、可能并发多源请求），消费方必须用定价锚（DOM 价 + 平台在售量）双重校验；② 同款皮肤不同平台在售量可差 42%——仅校验价格会漏判 Buff 串品（价差<1% 但在售差 42%）；③ discover 这类轻量批量路径要复用单品路径的脏价防护，否则同一数据源两套口径各坏各的。
- **v30（2026-08-07）**：PM 方案展示层落地（P0-P2，引擎零改动）——用户「以专业产品经理角度分析系统不足并提出优化方案」后按计划三批落地：
  - **买点接近度（程度可视化）**：`compute_buy_proximity` 对每个 buy 信号族算条件达标度 0-100%（几何平均，硬条件不达标归零），取最接近一族 + 缺口列表；单品报告「距买点最近信号」条 + 自选页 proximity 排序/徽章——解决「决策栏回调中/筑底看不出谁更接近买点」。
  - **决策条回测徽章（口径透明）**：按 ITEM_EXPECTANCY_STATS 族口径显示 14d 胜率 / 30d 期望 / 样本数，tooltip 含 95% CI——回答「阈值是否回测得到」。
  - **供给语义统一（矛盾消除）**：高位（分位>70%）供给收缩从「吸筹」降级为「锁仓诱多嫌疑」+ 风险提示，买点卡徽章同步；非 buy 态加 decision_note（需决策转 buy 后才可执行）——解决「高位泡沫+供给背离 vs 买点供给吸筹」矛盾观感。
  - **决策溯源/卖出位/术语表/引擎健康徽章**：报告折叠面板「为什么是这个结论」（拦截源中文映射）、持仓止损位（成本-25%）+ 90 日区间卡、术语速查、大盘页 J-2 三通道冻结监测徽章。
  - **数据闭环展示**：信号密度卡（最近 30 日 vs 历史分位）、持仓监控摘要条（接近买点 Top3 + 破位止损 Top5）、持仓关联最近 buy 信号、信号体检页 /checkup（C 通道月度胜率表 + 连续 2 月告警 + 实盘信号跟踪）、数据备份 API（SQLite online backup 到 data/backups）。
  - 顺带 ponytail 清理：删 volumes 链路签名参数（analyze_fresh/analyze_whale/_analyze_cycle/compute_trend_health）+ db.py MIGRATIONS 脚手架冗余，净 -286 行；修复本轮引入的 1 处注释乱码。
  - 验证：离线 smoke 67/0/6、三页面 + analysis 全字段 Jinja 渲染 PASS、编码健康 0 硬问题、CRLF/LF 行尾保持；参数冻结条款未触碰（决策逻辑零改动，仅展示层）。
  - 学到的新思路：① 决策条定性标签（筑底/回调）对用户不可操作，要给出「距可执行动作的距离」量化；② 展示层矛盾（吸筹 vs 高位泡沫）源于供给语义未按价格分位分层，须按位置分桶；③ 展示层统计必须与回放产物同源（期望徽章读 ITEM_EXPECTANCY_STATS，防漂移）。
- **v29（2026-08-07）**：Phase 3 重拟合流水线 + Phase 4 工程化落地——用户「继续 Phase 3 和 Phase 4」：
  - **Phase 3（REFIT-1）**：新增 
eferences/refit_pipeline.py → data/refit_pipeline_report.json（A2 三件套：walk-forward 70/30 切分 + 信号聚类 + 符号置换 p 值），
    默认读 signal_tracking v2 起点后新增信号（signal_date >= monitor_start）、--simulate 用 370 信号演练（train 69.4% / test 75.5% / p=0.001 / 达标，证明流水线可用）；
    J-2 overall.trigger_action 挂载「重拟合流水线已挂载」说明，触发后动作链闭环（J-2 触发 → 跑 refit_pipeline → 人工确认 → bump ENGINE_VERSION 复位）。
  - **Phase 4（工程化，零数据依赖可并行）**：每日备份（run_daily_collect 收尾调 backup_db → data/backup/ 保留 14 份）；批量扫描进度落盘
    （data/scan_progress_{id}.json，重启后仍可查询，写点全接 _persist / 读点磁盘兜底 / 过期同步删盘）；schema 版本化（schema_version 表 +
    SCHEMA_VERSION=1 + MIGRATIONS 版本驱动增量迁移脚手架，既有幂等 DDL 不动）；CI 离线流水线（.github/workflows/test.yml：ubuntu+py3.11+
    fastapi/uvicorn/multipart/jinja2，CS_MODEL_SKIP_NET 跳过 6 个网络测试，修 t_kline_daily 硬编码路径，3 个数据依赖用例加空库守卫）。
  - 验证：本地全量 70/3（csQAQ 网络测试环境 IP 绑定，非回归）、离线 CI 模拟 67/0/6、编码健康 PASS。
  - 学到的新思路：① 重拟合触发后动作链要在监测器里写明「已挂载的流水线」，J-2 触发才有明确下一步；② 工程化（备份/持久化/schema 版本化/CI）是不依赖数据的硬迭代，
    可与数据积累（A/B 通道）并行推进；③ 空库 CI 暴露的数据依赖用例用「空数据守卫」而非删断言，本地有数据时断言仍生效。
- **v28（2026-08-07）**：生产实盘信号跟踪落地（J-2 C 通道实盘化）——用户「对系统有价值就做」；把 C 通道从「370 信号回放近似」升级为「真实信号→真实结果」闭环：
  新增 signal_tracking 表 + `pipeline/signal_tracking.py`（record_buy_signal 去重 / backfill_signal_tracking 按 price_history 信号日后第 14/30 交易日回填、
  net 扣 2% 双边成本与回放同口径 / tracking_summary）；记录点覆盖统一分析核心 analyze_fresh（单品/搜索/自选）与批量扫描 _scan_item；
  run_daily_collect 每日收尾自动回填；j2_channel_monitor.py C 通道新增 production 段，dashboard 新增「C 生产实盘」行；
  顺带修复 db._init_schema 建表 DDL 未统一提交的隐患（236 行迁移 commit 之后的建表依赖调用方 commit，只读监控路径会回滚）；
  test_smoke 新增 t_signal_tracking（表存在 + 记录去重 + 回填口径硬校验）。
  学到的新思路：① 回放近似 → 实盘验证的关键是「信号当日记录 + 到期自动回填」的持续闭环，而非一次性回测；② schema DDL 必须自带 commit，只读监控脚本直连会暴露未提交隐患。
- **v27（2026-08-07）**：J-2 三通道监测落地（展示层）——用户反馈「弄不明白了，按最优路线落地」；调研确认 I-8 引擎层无可冒险改动
  （S3 强牛段当前门控 65=65 隐式全放行；S2 回踩族 C1 已证伪留研究池，A2 三件套未过不可上线），最优落地 = 把重拟合触发条件可视化：
  新增 `references/j2_channel_monitor.py` → `data/j2_channel_status.json`（A 恐慌事件 2/3、B 冻结后新数据 0/260 天约 2027-04-25、
  C 月度胜率监测 14d<80%/30d<55%/连续2月14d<70%，原始+去簇双口径，n>=10 才判定）；dashboard 数据积累进度卡旧 J-2 文案
  （「≥3 独立恐慌事件 + 2~3 中性回升段」）→ 三通道状态条（进度条+徽章+触发明细）；config.PARAM_FREEZE.triggers 补 A 通道、
  根/子 AGENTS.md 同步三通道；test_smoke 新增 t_j2_channel_status 硬校验（阈值/事件数同源/冻结起点天数）。
  学到的新思路：冻结期「落地」= 把触发条件做成可观测仪表而非调参；触发类监测必须给原始+去簇双口径与样本门槛，防单事件簇误触发。
- **v26（2026-08-07）**：第二轮复验收口（I-9 / I-7 / J-2 / I-8）——用户提出「恐慌独立事件本就少且不可控，计划落地；其他能落地的先做」：
  I-9 cap 复验（370 信号同口径：no cap +1118.94%/-44.68% → cap0.8 +193.30%/-9.39% 保持成立；族级细化候选 `cap0.8 + accumulate族0.5` +200.04%/-9.18%
  有正贡献信号，panic 族 0.3/0.4 收缩无收益贡献，记候选待 J-2 B/C 通道验证）；I-7 S3 分桶复验（恐慌桶 45 信号 win30 82.2% vs 非恐慌桶 67.8%，
  支撑恐慌桶保持 S3 优先级不降级）；I-8 强牛边界显式化（引擎零改动，S3 强牛段 16 信号 avg30 +52.39/win30 75% 支撑）；J-2 重拟合门槛改三通道
  （实测恐慌独立事件仅 2 个不可控：2025-10 五合一 + 2026-05 恐慌深跌；改为 A 事件≥3 自然积累 / B 新数据≥260 天（约 2027-04-25）/ C 胜率监测触发
  三通道任一满足，已写入 project-principles）。学到的新思路：复验结论落成「保持 / 细化候选 / 降级」三类可执行状态；依赖不可控事件的验收门槛
  必须给时间通道 + 监测通道替代，否则迭代永远在等；族级分类必须按 action_label 判定（signal_type 不准）。
- **v25（2026-08-07）**：旧引擎官方 88 基准终审删除——v24 刻意保留的 `data/item_backtest_latest.json` 本轮移除：
  先按引用图迁移 8 个引用点（`run_item_exit_backtest.py` / `run_item_9grid_backtest.py` / `methodology_report.py` / `tranche_fit*.py` /
  `trend_leg_validation.py` / `run_item_backtest_full.py` / `backtest-methodology.md`）→ 统一指向 `data/item_backtest_full_2025.json`（去量 v2，370 信号）；
  随后删除基准文件、移除 `run_item_backtest.py` 的 latest 写入（保留 `data/backtest_snapshots/` 日期快照）、清理基于旧基准生成的陈旧产物
  `item_9grid_backtest_latest.json` / `methodology_report.json`；同步 AGENTS / analysis-engine / decision-log。
  学到的新思路：「官方基准」类旧产物需整条引用链（读 + 写 + 生成产物）迁移后再删；生成型 JSON 产物残留基准死链，须随基准一并清理。
- **v24（2026-08-07）**：旧引擎（成交量时代）终审删除——用户确认后一次性清除 18 项：凭据
  `data/uu_headers.json`、采集器 `pipeline/collector_youpin.py`、旧 B1 结果 `b1_risk_validation.json`、
  旧审计 `legacy_audit.py/json`、成交量模拟研究 `sim_vol_replay.py` + 数据、旧统一大脑阶段1/2
  `unified_brain_*.py/json`、scripts-archive 5 个悠悠工具（依赖已删采集器）；同步根/子 AGENTS.md、
  SKILL.md、code_structure/PROJECT_STRUCTURE、plan-supply-price-v1（保留归档→终审删除）、
  notify_alert 示例文案。刻意保留 `item_backtest_latest.json`（旧引擎官方 88 基准，仍被
  `run_item_exit_backtest.py` 等 7 个脚本引用，删除需先迁移数据源）→ 学到的新思路：删旧引擎文件前
  必须按引用图走一遍；凭据类文件终审时优先删除（安全面）；「官方基准」类旧产物即使属旧引擎，
  被活脚本引用时删除会破坏研究可复现性，应先迁移再删。
- **v23（2026-08-07）**：旧引擎（成交量时代）残留审计与清理——复查发现活代码 1 个去量残留 bug：
  `webapp/analysis_service.py` DB K 线兜底把 `volume_total`（历史成交量）当 `in_sale_count` 传入引擎
  （供给检测被污染）→ 改读 `in_sale_count` 列；同步 4 处过时注释（portfolio_risk/dashboards/trend_health）
  与 4 个活文档（SKILL/code_structure/PROJECT_STRUCTURE/子 AGENTS.md）的去量口径；清理 3 个回放临时日志
  （`_baseline*.log`/`_devol_run.log`，正式 JSON 保留）。核实为刻意保留：collector_youpin.py + uu_headers.json
  （项目决策归档，uu_headers 为敏感凭据建议后续删）、旧回放/研究数据与 references 旧研究脚本（历史证据）
  → 学到的新思路：去量落地必须复查数据读取点（SELECT * 兜底路径），而不只是采集/写入点；活文档
  （SKILL/结构文档）与代码同步要纳入残留审计。
- **v22（2026-08-07）**：研究工程收口 · 期望统计单一事实源 + 基准对照 + 参数冻结（③④⑤）——
  新增 `references/sync_expectancy_config.py`（回放产物 → `config.ITEM_EXPECTANCY_STATS` 自动同步，
  防手工双写漂移，`t_expectancy_sync` 全字段硬校验）、`references/benchmark_compare.py`
  （策略 cap0.8 vs 池内等权买入持有 vs 大盘指数，full/active 双窗口，产出 `data/benchmark_compare.json`）；
  基准发现：策略 +193.30%/-9.39% 相对大盘 -4.02% 大幅超额，但绝对收益低于池内等权 +509.75%/-54.12%——
  引擎边际价值在风险控制（maxDD 9.4% vs 54~58%）而非裸多收益；`config.PARAM_FREEZE` 参数冻结条款
  （冻结去量 v2 全参数 + cap0.8 + 单票 30% + 期望统计口径，~260 天新数据约 2027-04-25 后真 OOS 复验，
  `t_param_freeze` 测试防删）→ 学到的新思路：① 基准必须给「满仓被动 + 大盘指数」双参照系，避免把跑赢指数
  误判为策略成功；② 跨文件双写统计必须脚本化同步 + 测试硬校验，人肉同步必然漂移；③ 参数定稿要落成可测试的
  冻结条款（代码级），不能只写在文档里。
- **v21（2026-08-07）**：分析路径重构 · 抽公共分析服务层（工程）——新增 `webapp/analysis_service.py`（7 个助手迁移 + 统一核心 `analyze_fresh` + 共享 ctx），`webapp/main.py` 2439→1816 行（-623），api_items_search/analyze/watchlist_analyze 三条 ~90% 重复路径各缩至 ~30 行；批量扫描/发现任务改调用服务层助手；watchlist 历史口径差异用显式参数钉住（apply_anchor=False / volumes_from_bars / allow_single_price），顺带修复 search 重复保存 analysis_result 冗余；test_smoke 58/61 无回归 → 学到的新思路：① 重构行为保持要用显式参数把跨路径历史差异钉成可审计开关，别顺手改行为；② 全局字符串改名要防误伤测试函数名（t_kline_price_sane → tkline_price_sane 事故）。
- **v20（2026-08-07）**：B1 风险预算层 v2 复验（引擎升级 → 风险参数重验）——去量引擎 370 信号跑同款组合模拟（`references/b1_risk_backtest_v2.py`，结果 `data/b1_risk_validation_v2.json`）：cap0.8 强化（+193.30%/-9.39%，旧 +54.6%/-15.3%）；**熔断 10% 失效**（全量 0 触发、子集微负），语义降级为「信号质量劣化监测器」（实盘回撤破 10% = 实盘偏离回测，触发检查而非暂停买入）；dd5% 破坏性禁用；dd8% 压回撤但付 ~28pp 收益仅监控；单票 10% 硬上限二次证伪维持只提示 → 学到的新思路：① 风险预算参数是信号结构的函数，引擎升级后必须重验（旧 B1 的深值 241 信号结构已不存在）；② 失效的风控规则可转化为质量监测器而非删除；③ 复验必须同款 simulate 只换输入，保证口径可比。
- **v19（2026-08-07）**：P0 数据层去量 + P3 在售量增强 + P4 解冻全面落地（批次执行）——webapp 移除全部悠悠采集函数（5 函数/5 调用/import），进度卡「真实成交量」→「在售量覆盖」（in_sale_count），健康检查成交量项 → 在售量项；`run_daily_collect` 删除 `collect_volume`、K 线全量刷新周日 → **每日无条件执行**（SQL 无自选过滤，补齐非自选品价格+在售量停更缺口）；P4 解冻：item_analysis 退出参数定稿注释、「量能确认 v2」关闭标注；test_smoke 删 4 个 youpin 用例、`t_is_sunday_order` → `t_kline_daily`（断言 is_sunday 已移除）。收尾：临时补丁脚本清理、文档全同步。详见 `references/plan-supply-price-v1.md` §9.1 → 学到的新思路：数据层「停用」要落到 import/函数/测试三级编译级移除，避免半死代码；采集刷新周期按数据消费者缺口定（非自选品价格行停更→每日全量），而非成本习惯（每周日）。
- **v18（2026-08-07）**：去量引擎 v2 = v1 + I-13 深值大盘上涨禁买——v1 回放对比基线（96品池，2025-01-01~2026-08-05，warmup30，cost2%）：452 vs 450 信号，win14 65.5 vs 64.9%、avg14 13.85 vs 13.95 → 去量本身中性，期望基本持平；宫格分析（pct×供给×大盘 chg30）定位 deep_value 毒区：**大盘 chg30∈[3,10) 上涨段 68 信号 win14 仅 40%/avg+0.8**（横跨10个月40品、基线同款、2025 44%/2026 43% 年度独立验证，非事件簇），语义=大盘上涨时单品掉队是死钱，「深值·大盘企稳」应只在大盘企稳/修复环境（mchg30<=-3）出手；deep_dip 同闸门回测为负优化（mchg30>=3 剔除 9 信号 avg+4.5，含两个右尾赢家，净损+40.6 期望）→ 仅改 deep_value 触发为 `mchg30<=-3`；**v2 回放 370 信号：win14 71.1%（+5.6pp）/avg14 16.70（+2.85）/wavg14 20.76/win30 72.1%/avg30 26.54**，deep_value 族 56 信号 75%胜率/+14.9（原 56%/+7.0），前后半段 63%/79% 均强 → 学到的新思路：删除近盈亏平衡的磨损信号比增加信号更提升期望；大盘环境是深值族的首要条件变量、估值深度其次；族级闸门证据需跨年+跨引擎双重稳定才上线。
- **v17（2026-08-07）**：废除成交量 · 以在售量+价格重构（用户决策）——证据链：sim_vol 激活实验 458→474 期望持平略降（avg14 -0.38/avg30 -0.69）、引擎基线本就 volume=0 占位、90 天积累因「UPDATE 不 INSERT + 非自选品价格行停更」不可达且卡住引擎冻结期 → **彻底放弃悠悠成交量采集**（采集/进度卡/健康检查全移除），引擎四处去量触点重分配（流动性量40%→价差+在售深度、趋势量价16%→动量/趋势、动量3x触发删除、周期量能→在售量萎缩），供给侧以 in_sale_count（csQAQ chart 自带、94品×约1年、无需登录态）为核，新增「在售量×价格」组合宫格；验证=458 基线零差异复现+真实量消融；详见 references/plan-supply-price-v1.md → 学到的新思路：数据因子立项前必须先做「激活消融」验证增量信息；依赖登录态的采集因子默认不立项；引擎冻结期必须带退出条件。
- **v16（2026-08-06）**：P2 开箱量因子预研（研究先行·结论暂缓）——接口实测（stat/case/chart **仅 32 天**历史、info/good container 皮肤→箱映射链路可用、info/roi 478 箱当前快照）；32 天方向性研究显示开箱量为**同步/滞后热度指标**（日变化 vs 次日大盘 r≈0、vs 未来 3 日 r=-0.24 不显著；箱开箱突增后单品 fwd3/fwd7 与基线无一致差异，噪声内）；自选 104 品中仅 14 个能映射到有开箱数据的箱（活跃箱交集≈0）→ **不投产因子、不建采集模块**，记录预研 → 学到新思路（数据可用性必须实测确认；热度类因子与情绪信息同质，须先问边际信息；单 regime 事件研究无法分离效应与均值回归）→ 新增 **I-12** 观察项。
- **v15（2026-08-06）**：J-3 信号族样本深度落地 + P1 执行参考价——升级 j1_event_counts.py 为 K-2 引擎回放同源（消除旧文件依赖/口径漂移），进度卡新增「信号族样本深度」（各族信号数/独立事件数/14d胜率 + J-2 门槛进度）；同步 config.ITEM_EXPECTANCY_STATS 的 win/avg/ci 至同源（旧值两次漂移）；执行记录成交价留空时带出 price_history 锚定收盘参考价（ref-price 接口，只带出不写库，持仓均价仍以用户确认为准）；test_smoke 新增 families 同源硬校验 → 学到新思路（展示统计必须以回放产物为唯一同源、手工同步常量会漂移；执行闭环的"真实价"瓶颈在历史逐日成交价，锚定收盘价是当前最优参考，成交量积累后可升级悠悠成交均价）。
- **v14（2026-08-06）**：csQAQ 接口迁移（数据源修复）——绑定 IP 后旧直连端点仍 401，确诊为接口改版永久废弃：search_items→`/search/suggest`、hash→id→`/goods/getPriceByMarketHashName`、详情→`/info/good?id=`（goods_info 直取盘口/日量，去掉 422 的 info/chart 调用）；新增 `bind_local_ip()`（POST /sys/bind_local_ip）并接入 `run_daily_collect` 每日自动重绑；大盘 kline / current_data / volume 实测不受影响；test_smoke 61/61 → 学到新思路（数据源是工程的硬依赖：接口改版会静默破坏采集，需要「每日 IP 重绑 + 直连接口回归硬校验」守护 → I-11）。
- **v13（2026-08-06）**：模拟量激活实验 + 趋势腿边界验证——2025 全年回放核实（25 牛市段 deep_value 49 信号 win14 38.8%/avg -0.22 = 抄底族牛市弱点量化，非「零成绩」；458 信号中 S3 趋势腿已占 37%（170 信号 30d +23.52））；S3 强牛边界 sent<40+TH≥60 回放 30d +46.28（引擎已隐含放行，零改动仅显式化）；模拟成交量（真实在售量×周转率基准，references/sim_vol_replay.py）回放 458→474 期望持平略降（avg14 -0.38 / avg30 -0.69，净新增 29 个 th=35 边缘 deep_value 质量差 win30 39.3%）→ 结论：模拟量只激活链路不带来信息，**不污染生产基线**，真实量 90 天到位直接切换 + 趋势腿量能确认 v2 → 学到新思路（「无成绩」≠「零信号」；族分布须按触发通道拆解；模拟数据可证伪「激活即更好」）。
- **v12（2026-08-06）**：系统整理——清理过时数据与文档：删除 26 个日志/可再生的回放临时产物（advice/deepvalue/topup replay、全部 *.log，均 gitignore 覆盖）；git rm 3 个一次性回填报告（backfill_csqaq365_report/backfill_youpin_report/sample_collect_report）；修复失效引用（references/backtest_results.json 已于 12ea421 移除 → 统一指到 data/item_backtest_full_2025.json）；analysis-engine/market-engine/backtest_layered 标注 superseded 并指向 engine-unified + th_calibration；item-sample-plan 状态头改「已执行」；AGENTS.md 补 TH 三区语义 + 超跌例外数据口径。test_smoke 61/61 → 学到新思路（清理前先核对 gitignore 与代码引用：scan_history/ 被 webapp 信号中心使用、backup/ 是合并操作回滚点，均保留；回放临时产物脚本可再生成故直接清）。
- **v11（2026-08-06）**：TH 矫正预研与落地——TH 三区语义确立（<35 恐慌黄金坑 / 35-54 摩擦带 / >=55 趋势确认，解释层）；X2（deep_value 改 TH 门槛）被回放证伪（avg14 12.35→11.78，离线桶≠引擎族口径）；C2 落地 = I-6 部分（deep_value 加大盘 chg30 闸门排除 [-3,3) 横盘段，回放 win14 +2.0pp / avg14 +1.20 / wavg14 +1.09 前后半段一致）；豁免候选全失败保持一刀切；展示层距买点 TH 三区化 → 学到新思路（TH 三区是解释层语义，引擎层由族注册制覆盖；TH 门槛直接改需引擎回放验证，离线桶口径不能替代）。
- **v10（2026-08-06）**：黑天鹅事件日历落地——新增 EVENT_CALENDAR（纪念品炼金 05-25 / 黄盾 07-16 / 五合一崩盘 10-24）+ historical_event_impact() 标注 + 信号复盘「事件标注」列；**10-14~18「崩盘前夜陷阱」修正为五合一黑天鹅外生冲击**（S3 剔除事件影响后干净 52 信号 win30 90.4% / avg +44.41%，受影响 13 信号中 8 个 fwd30 全落崩盘期 avg -17.3%）；test_smoke 61/61 → 学到新思路（牛市/熊市只是市场数据体现，最终目标是符合整个市场的投资工具；趋势腿不落地会踏空下一轮牛市 → I-8 提升为战略级；事件影响应从策略评估中剔除：标注而非调参）。
- **v9（2026-08-06）**：数据对接修复——信号复盘升级 K-2 503 信号（88 旧基准废弃）、期望统计三键重算（panic 92/2事件、deep_value 211/33、accumulate 200/23）、死文件清理（backup_price_history_20260802/topup_replay_tmp）、test_smoke 新增数据对接硬校验（60/60）→ 学到新思路（展示层统计常量必须与回放产物同源，用测试固化防漂移）。
- **v8（2026-08-06）**：K-2 守卫统一实验落地——deep_value 叠加供给扩张闸门（剔除 91 个供给扩张坏信号，被剔 14d avg -1.30），
   回放 581→503：win14 57.8→62.2%、avg14 +10.4→+12.35、wavg14 +15.73→+17.41、win30 62.8→66.0%，前后半段一致→ 学到新思路（**去重释放效应**：闸门剔除会释放 7 天去重链，转出 13 个 deep_dip 信号 avg +4.0，离线模拟低估剔除净收益；deep_dip 豁免在供给扩张 35~120% 下仍放行，n=13 待 J-2 复验）。
- **v7（2026-08-06）**：阶段4 展示层接入——状态桶标注上线单品报告决策条（引擎口径），展示层恐慌判定 80→75 对齐引擎（I-1 遗留修正），判定收敛到 market_context.state_bucket() 单一口径源→ 学到新思路（状态口径必须单一来源，展示/补仓/引擎三层各有口径，改动前先分清哪层是回测参数）。
- **v6（2026-08-06）**：统一大脑阶段3 架构重构落地（信号族注册制 + 六态状态桶 + 统一决策核心，581 信号全字段保真零差异）→ 学到新思路（后置族绕过守卫2/供给过滤的闸门不对称性、新族注册成本归零），新增 K-2 映射。
- **v5（2026-08-06）**：统一大脑方案建立（引擎结构整理文档 + 阶段1 条件期望表 + 581 信号全窗口回放）→ 阶段2 期望排序决策层被样本外证伪（期望跨时段漂移）→ 新增 K-1 映射；阶段 3 聚焦架构统一不做仓位分档。
- **v4（2026-08-06）**：牛市段趋势腿研究（2025-01-01~10-31 同物品池）→ 学到新思路（S3 强牛段有效但崩盘前夜误触发；S2 牛市段=回调腿），细化 I-8 + 新增 I-10 观察项。
- **v3（2026-08-06）**：J-1 胜率事件上下文落地 → 学到新思路（n 大≠事件多：deep_value 241=13 事件、topup_ok 448≈9 事件、base 46=10 事件），新增 J-3 映射；第一批全部完成。
- **v2（2026-08-06）**：第一批 I-3 存量审计完成 → 学到新思路（事件级样本是硬约束），新增 J-1/J-2 两项映射；I-1/I-2/I-4 完成。
- **v1（2026-08-06）**：初始基线。9 项映射源自 2026-08-05 C1/B1 执行中沉淀的 6 条新思路（见 decision-log「C1 落地」「B1 风险预算层」）。

## 新思路 → 迭代项映射

| # | 新思路（来源） | 迭代项 | 批次 | 依赖 | 状态 |
|---|----------------|--------|------|------|------|
| 1 | chg30 是补仓唯一稳健分类变量（V型底/阴跌中继回测） | **I-1** 大盘状态标注增强：市场环境条区分「恐慌+深跌=V型底区 / 恐慌+中跌=阴跌中继区」 | 第一批 | 无 | 完成 |
| 1 | 同上 | **I-6** 引擎侧推广验证：buy 信号族是否也该用大盘 chg30 区分 V型底/阴跌中继（回测先行） | 第二批 | 更多事件样本 | **部分完成（deep_value 阴跌闸门，2026-08-06，其余族待复验）** |
| 2 | 硬性风控参数必须组合回测先行（单票硬上限被回测推翻） | **I-2** 风控参数上线标准固化进 project-principles：新硬性风控参数必须过组合回测，否则只做提示；B1 参数列入月度敏感性复跑 | 第一批 | 无 | 完成 |
| 3 | S3 在恐慌桶也正期望（+19~22%） | **I-7** S3 分桶复验：样本扩大后按恐慌/非恐慌桶对比 S3 期望，决定是否在恐慌桶提升触发优先级 | 第二批 | 无（370 信号现成） | **完成（2026-08-07，恐慌桶 30d 胜率 82.2% 支撑优先级不降级）** |
| 4 | A2 教训：全窗口均值好看 ≠ 可落地（S2 被 OOS 拦下） | **I-3** 存量信号族 A2 审计：对 base/panic/deep_value/P1-0 四族做 walk-forward + 聚类 + 置换检验，揪出靠单一事件簇撑胜率的族 | 第一批 | 无（数据现成） | 完成 |
| 5 | 熔断管组合闸门、信号管单品买点，两层互补 | **I-4** 风控分工文档化：组合闸门（熔断/敞口）+ 信号族闸门（未来路由）+ 单品买点（引擎）三层职责写进 AGENTS.md | 第一批 | 无 | 完成 |
| 5 | 同上 + 牛市段研究（2026-08-06）：S3 强牛段有效(TH≥60/bull +38~43)但崩盘前夜误触发；S2 牛市段=回调腿(bear/th45-60/chg30-8~0 最强, bull桶平庸) | **I-8** 路由层最终落地：强牛腿边界显式化——bull/TH≥60 放行 S3、S2 仅开回调桶、sideways 禁入 | 第二批 | 无（370 信号现成） | **显式化完成（2026-08-07，引擎零改动）** |
| 6 | S3 单日峰值 11 信号 × 0.10 = 1.1 仓位自然触发 0.8 cap | **I-9** 并发 cap 复验：S3 信号密度随样本上升后重跑组合回测，确认 0.8 cap 或按信号族分组细化 | 第二批 | 无（370 信号现成） | **完成（2026-08-07，cap0.8 保持；族级细化 accumulate 0.5 记候选）** |
| 7 | I-3 审计：panic 42 信号=1 次事件、base OOS 14d 17%、deep_value p 不显著（事件级样本是硬约束） | **J-1** 胜率展示带事件上下文：信号中心/报告显示「胜率（N 次独立事件，去簇后）」，避免单事件高胜率误读 | 第一批 | 无 | 完成 |
| 7 | 同上 | **J-2** 重拟合触发三通道：A 独立恐慌事件≥3（自然积累）/ B 新数据≥260 天（约 2027-04-25）/ C 胜率监测（buy 连续 2 月 14d<70% 或月度 14d<80%/30d<55%），任一满足即重拟合（已写入 project-principles） | 第二批 | 无（已改三通道） | **完成（2026-08-07，三通道监测 + C 生产实盘信号跟踪已落地）** |
| 8 | J-1 落地：n 大≠事件多（deep_value 241=13 事件、topup_ok 448≈9 事件、base 46=10 事件） | **J-3** 各族样本深度并入数据积累进度卡：展示「信号数/独立事件数」随采集增长，作为数据到位可视指标 | 第二批 | 无（数据现成，低成本展示） | **完成（2026-08-06）** |
| 10 | 阶段2 证伪：条件期望跨时段漂移，期望排序/分档/cap剔除样本外失效 | **K-1** 期望排序类决策重估：J-2 B/C 通道达成后，用 walk-forward 重跑「期望分档/排序」并对比触发条件筛选；当前只做架构统一 | 第二批 | J-2 B/C 通道（≥260 天 / 胜率触发） | 待做 |
| 12 | K-2 落地后展示层仍挂旧数据（复盘 88 信号/期望条旧切片） | **K-3** 展示统计与回放产物同源：复盘/期望条/事件数统一读最新回放，test_smoke 固化「展示 n = 回放族计数」硬校验 | 第一批 | 无（数据现成） | **完成（2026-08-06）** |
| 11 | 阶段3 落地：后置族（deep_value/panic_easing）自带条件但**不经过守卫2/供给扩张闸门**（旧链路保真） | **K-2** 守卫统一实验：deep_value 叠加供给扩张闸门（剔除 91 坏信号 14d avg -1.30，回放聚合全维度改善，见 decision-log）；supply_accum/panic_easing 经预研不叠加（期望反降/消灭信号） | 第二批 | 无（581 信号预研 + 引擎回放） | **完成（2026-08-06）** |
| 9 | 黑天鹅事件日历（2026-08-06）：10-14~18 是五合一黑天鹅外生冲击（黄盾 07-16 / 纪念品炼金 05-25 同批标记），非策略误判 | **I-10** S3 崩盘前夜标注（非陷阱守卫）：复盘/回测对 fwd 窗口与事件影响期重叠的信号自动标注（EVENT_CALENDAR + historical_event_impact），外生冲击不算策略负贡献；不调参 | 第一批 | 无（数据现成） | **完成（2026-08-06）** |
| 13 | 「牛市先不着急做吧」已过期（2026-08-06）：牛熊只是数据体现，最终要做符合整个市场的投资工具 | **I-8** 趋势腿落地优先级提升（战略级）：独立事件门槛（J-2，去量后不再等成交量 90 天）数据到位后优先落地趋势腿，避免下一轮牛市踏空 | 第二批 | J-2 B/C 通道（≥260 天 / 胜率触发） | **显式化完成（2026-08-07，引擎零改动，接线待数据）** |
| 14 | 数据源是工程的硬依赖（2026-08-06）：csQAQ 接口改版导致旧直连端点绑定 IP 后仍 401，采集静默失效 | **I-11** 数据源接口回归守护：`bind_local_ip()` 每日自动重绑（已入 run_daily_collect）+ test_smoke 直连接口硬校验（搜索/详情/hash 解析），接口变更时第一时间暴露 | 第一批 | 无（已落地） | **完成（2026-08-06）** |
| 15 | 开箱量预研（2026-08-06）：开箱量是同步/滞后热度指标非领先指标；历史仅 32 天；自选品与活跃箱无交集 | **I-12** 开箱量因子观察项：若未来自选覆盖活跃箱产物（开箱量有数据），再以 stat/case + info/roi 每日快照补采积累，90 天后验证「开箱突增→价格」边际信息；当前不投产 | 第二批 | 自选品结构变化 | 待做（低优先级观察项） |
| 16 | 第一性分析（2026-08-08）：executions 表/API 已建但仅 1 条手动记录，执行环摩擦大、反馈不可见 | **F-1** 报告页「按建议记录执行」一键入口 | P0（第一性） | 无（表/API 现成） | **完成（2026-08-08，共享 exec-modal）** |
| 16 | 同上 | **F-2** 组合仪表盘执行复盘对照卡（真实 pnl/滑点/胜率 vs 纸面） | P0（第一性） | 无（数据现成） | **完成（2026-08-08，/api/executions/review + watchlist 对照卡）** |
| 16 | 同上：发现机会依赖人工选品，覆盖 26/104 | **F-3** discover 全量重扫扩池 | P0（第一性） | csQAQ 限流 | **完成（2026-08-08，复用优先 + 扩池落地，items 110→136）** |
| 16 | 同上：回测统一扣 2%，无价差/流动性冲击 | **F-4** 成本建模进回测（价差+手续费+冲击，in_sale_count 估） | P1（第一性） | 执行数据积累 | 待做 |
| 16 | 同上：cap0.8 只是并发上限，无资金分配/相关性/再平衡 | **F-5** 组合模拟器（研究层，待数据评估后议是否进引擎） | P1（第一性） | 执行数据积累 | 待做 |
| 16 | 同上：事件驱动缺失（I-12 已暂缓） | **F-6** 事件驱动提示（Major/开箱/新箱，非决策） | P2（第一性） | — | 待做（观察项） |
| 16 | 同上：推送已打通，执行可移动化 | **F-7** 钉钉卡片一键执行/移动端（二次确认+幂等） | P2（第一性） | 资金安全设计 | 待做 |
| 17 | 四模块第一性原理审计（2026-08-11，`first-principles-modules-fit.md`）：抛压衰竭 55/70/85 阈值与 buy 接线从未回测（decision-log 0 记录） | **M-1** 抛压衰竭大盘择时检验：market_index 逐日复算 sp 分桶（sp>=85）vs 同跌幅对照（drop20<=-7 且 sp<85），三件套；涉及 `market_th.py:527-537` buy 接线参数须先过三件套 | 第三批（审计立项） | 无（market_index 现成） | 已立项待执行 |
| 17 | 同上：composite 乘子（估值折价/TH 偏移/动作加分/数据质量）无回测，TH 偏移方向疑与「拉升/追高最差」定论相反、位置双计权 | **M-2** composite 消融检验（优先）：回放信号复算 composite 各变体分桶 14d 胜率单调性，产物 `data/_exp_composite_ablation.json` | 第三批（审计立项） | 无（回放产物 + 特征复算） | **完成（2026-08-11，TH 偏移反向落地 M-5）** |
| 17 | 同上：discover 追踪 fwd14 不扣 2% 成本、市场弱过滤方向疑与「洗盘最优」张力、候选空间限 13 武器 | **M-3** discover 发现榜 alpha 检验：discover_history 归档结算 net2% vs 池内等权；候选扩展（印花/收藏品）另行确认 | 第三批（审计立项） | discover_history 积累（现 5+ 份，n 小） | 已立项待执行 |
| 17 | 同上：执行复盘 real（日历日）vs paper（交易日）结算口径不一致、action 混用 | **M-4** 执行复盘口径量化：real vs paper 结算差分布 + action 分层统计 | 第三批（审计立项） | executions>=20（A1-4） | 已立项待执行（等待数据） |

## 批次

- **第一批（2026-08-06 开工，零数据依赖）**：I-3 存量审计 → I-1 大盘状态标注 → I-2 + I-4 文档固化 → J-1 胜率事件上下文（完成，低成本展示项）。
- **第一批剩余**：无（第一批全部完成）。
- **第二批（2026-08-07 复验收口）**：I-7 S3 分桶复验、I-8 强牛边界显式化（引擎零改动）、I-9 cap 复验、J-2 三通道修订 + 监测落地 + C 通道生产实盘信号跟踪 全部完成；I-6 其余族复验保留（deep_value 闸门已落地 2026-08-06）；剩余 K-1（期望排序重估，依赖 J-2 B/C 通道）与 I-12（开箱量低优先级观察项）继续观察，不阻塞。 **Phase 3/4（重拟合流水线 + 工程化）已于 2026-08-07 完成（见 v29），第二批闭环。**
- **P0-P2 第一性原理落地（2026-08-08 立项）**：P0 F-1 一键执行 + F-2 执行复盘对照卡 **已完成（2026-08-08，见 v43）** → F-3 采集复用优先（不重复采集）**已完成（2026-08-08，见 v44）**；
  P1 F-4 成本建模进回测 + F-5 组合模拟器（研究层，依赖执行数据积累）；P2 F-6 事件驱动提示 + F-7 钉钉卡片一键执行（体验层）。
  全部不触碰冻结参数（仅数据/展示/调度/采集层改动）。
- **第三批 · 审计立项（2026-08-11）**：M-1 抛压衰竭择时（阶段 0 预跑完成，待事件积累复验）、M-2 composite 消融（完成 → M-5 落地）、M-3 discover alpha（已立项待执行，依赖 discover_history 积累）、M-4 执行复盘口径（已立项待执行，依赖 executions≥20）、M-5 TH 偏移反向（完成）、M-6 discover 候选扩展（阶段 1 落地）。**第三批闭环项 = M-3（与贴纸分桶合并）。**
- **第四批 · 量化专家框架（2026-08-12 立项，P1）**：执行序 4 → 1 → 3（并行）→ 2——① 三要素元数据入册（0.5-1 天，贴纸 A2 注册前置）→ ② C 通道族级失效监测（1-2 天，串行依赖 ①）→ ③ 去簇胜率主视图对照（0.5 天，展示层，独立并行）→ ④ 前后半段一致性制度化（流程/文档，先行）。**第四批 4 项已全部完成（2026-08-12，见 decision-log「第四批 · 量化专家框架落地」）**；贴纸阶段 2（品类分桶追踪，并入 M-3）继续并行积累。

## 状态追踪

- I-3 存量信号族 A2 审计：**完成（2026-08-06）**——panic 42 信号集中在 2026-05-22~05-26 单次事件；base OOS 14d 17%（回落 51pp）p 不显著；deep_value OOS 14d 30% p=0.729；P1-0 引用 S3 检验（单簇 91.5% 已去重）。详见 data/legacy_audit.json。
- I-1 大盘状态标注增强：**完成（2026-08-06）**——market_regime 六态标注接入大盘仪表盘与批量扫描市场环境条。
- I-2 风控参数上线标准 + I-4 三层闸门分工：**完成（2026-08-06）**——写入 project-principles.md / AGENTS.md。
- J-1 胜率事件上下文：**完成（2026-08-06）**——各族独立事件数固化（panic 42=1、base 46=10、deep_value 241=13、topup_ok 448≈9、p10=48 交易日），单品报告/补仓建议展示「N 次独立事件」；生成脚本 references/j1_event_counts.py → data/signal_event_counts.json。
- 牛市段趋势腿研究（2026-08-06）：**完成**——同物品池对比，S3 牛市窗 30d +38.4（主簇 8-9 月 52 信号 90% win 跨事件验证）、S2 +22.7（回调桶最强）；发现 S3 崩盘前夜陷阱与强牛段门控缺口。详见 trend_leg_research.md §9。
- 统一大脑阶段3 架构重构（2026-08-06）：**完成**——pipeline/item_analysis.py 串行 if 链重构为信号族注册制（SIGNAL_FAMILIES）+ 六态状态桶（_state_bucket）+ 统一决策核心（decide_fusion_signal）+ 12 闸门注册表（_GUARDS）；全窗口回放 581 信号全字段零差异，test_smoke 59/59；详见 engine-unified.md §5。
- 统一大脑阶段4 展示层接入（2026-08-06）：**完成**——单品报告决策条显示市场状态桶徽章（fusion_decision.state_bucket，引擎口径 75）；展示层 market_regime 恐慌判定 80→75 对齐；状态桶判定统一到 market_context.state_bucket()（item_analysis 与 batch_scan 共用，杜绝口径漂移）；补仓引擎 80 分恐慌阈值保持不动（回测参数）；test_smoke 通过。
- 统一大脑方案（2026-08-06）：**阶段1 完成**（engine-unified.md + 条件期望表）；**阶段2 完成**——期望排序决策层被样本外证伪（详见 decision-log），阶段 3 架构重构待用户确认。
- 黑天鹅事件日历（2026-08-06）：**完成**——EVENT_CALENDAR 三事件 + historical_event_impact() + 信号复盘「事件标注」列（受事件影响：五合一崩盘 等）；10-14~18 由「陷阱守卫」修正为外生冲击标注；test_smoke 61/61。详见 decision-log「黑天鹅事件日历落地」。
- I-10 S3 崩盘前夜标注：**完成（2026-08-06）**——改为事件标注非陷阱守卫。
- I-8 趋势腿落地：**显式化完成（2026-08-07，引擎零改动）**——强牛腿边界规则（bull/TH≥60 放行 S3、S2 仅开回调桶、sideways 禁入）经 370 信号验证：S3 强牛段（sent<40+TH>=60）16 信号 avg30 +52.39/win30 75%；路由层接线待 J-2 B/C 通道数据到位后落地，避免踏空下一轮牛市。
- I-6 引擎侧 chg30 区分：**部分完成（2026-08-06）**——deep_value 族加大盘 chg30 闸门排除 [-3,3) 横盘段（回放 win14 +2.0pp 前后半段一致），其余族待 J-2 数据复验。详见 th_calibration.md §6。
- J-3 各族样本深度并入进度卡：**完成（2026-08-06）**——j1_event_counts.py 升级为 K-2 回放同源，进度卡新增信号族样本深度（信号数/独立事件数/14d胜率 + J-2 门槛）；config.ITEM_EXPECTANCY_STATS 同步同源。
- I-7 S3 分桶复验：**完成（2026-08-07）**——吸筹族 166 信号按恐慌/非恐慌桶对比：恐慌桶 45 信号 win30 82.2%/avg30 +21.6 vs 非恐慌桶 121 信号 win30 67.8%/avg30 +25.12；恐慌桶 30d 胜率 +14.4pp 显著更高，支撑恐慌桶保持 S3 优先级不降级。详见 data/s3_bucket_replay.json。
- I-9 并发 cap 复验：**完成（2026-08-07）**——370 信号同款组合模拟：no cap +1118.94%/-44.68% → cap0.8 +193.30%/-9.39% 保持成立；族级细化候选 `cap0.8 + accumulate族0.5` +200.04%/-9.18%（收益 +6.7pp、回撤基本持平），panic 族 0.3/0.4 降收益不降回撤；族级分类须按 action_label 判定。详见 data/cap_family_backtest.json。
- J-2 重拟合触发条件：**完成（2026-08-07，三通道监测 + C 生产实盘信号跟踪已落地）**——恐慌独立事件实测仅 2 个（2025-10-24 五合一 1 个 + 2026-05-22~31 恐慌深跌 91 个），原「≥3~5 独立恐慌事件」门槛不可控；改 A 事件≥3 自然积累 / B 新数据≥260 天（约 2027-04-25）/ C 胜率监测触发三通道任一满足；监测 `references/j2_channel_monitor.py` → `data/j2_channel_status.json`（A 2/3、B 0/260、C 月度 14d/30d 胜率 原始+去簇双口径 n>=10 判定 + production 实盘统计）接入数据积累进度卡；C 通道实盘化：signal_tracking 表记录生产 buy 信号，14/30 交易日后按真实价格回填（pipeline/signal_tracking.py，每日采集自动回填）；事件不足时参数冻结，允许研究池 + 展示层探索，事件级验证用「市场事件 + 跨年度 walk-forward」替代统计功效门槛；事件簇纪律保持（±3~7 天簇限次）。已写入 project-principles。
- Phase 3/4（REFIT-1 重拟合流水线 + 工程化）：**完成（2026-08-07）**——refit_pipeline.py 挂载 A2 三件套（walk-forward/聚类/置换 p 值），
  simulate 演练 train 69.4%/test 75.5%/p=0.001/达标；每日备份/扫描进度落盘/schema 版本化/CI 离线全落地；J-2 trigger_action 动作链闭环。
- 其余（K-1）：待做（第二批剩余，依赖 J-2 B/C 通道：≥260 天新数据或胜率监测触发）。
- 参数冻结期解除：**完成（2026-08-10）**——`PARAM_FREEZE` → `PARAM_REGIME` 参数治理台账，J-2 三通道监测照常收集（提示项，非禁止调参闸门）；参数迭代纪律 = 回测先行 + 三件套记录 + 文档同步；详见 decision-log「解除参数冻结期」。
- 四项审计落地：**完成（2026-08-10）**——周期权重反转 / panic 分级仓位修复 / 概率去 z 化落地，供给降仓经组合模拟证伪不落地；回放基线改 365 天窗口（332 信号），产物与台账、AGENTS.md、decision-log 全量同步；详见 decision-log「四项审计落地」。
- I-12 开箱量因子观察项：**待做（低优先级）**——预研结论暂缓（2026-08-06），详见 decision-log「P2 开箱量因子预研」。
- F-1 一键执行录入 / F-2 执行复盘对照卡：**完成（2026-08-08）**——共享 exec-modal 全站接入 + 报告页一键按钮；/api/executions/review + watchlist 真实 vs 纸面对照卡。
- F-1.1 信号→动作统一映射：**完成（2026-08-08）**——watch 类信号→观望（不再硬映射建仓），单品报告建议动作徽章 + 批量扫描动作徽章 + exec-modal 观望选项。
- F-1.2 任意操作均可记录：**完成（2026-08-08，F-1.3 修正）**——EXEC_ACTIONS 加 hold（API 兼容）；F-1.3 定稿：观望类无按钮、只记录有意义的操作（建仓/补仓/减仓/清仓），相反操作用自选页手动录入；删除 batch_scan_latest.json 旧缓存（含 F-1.1 前 watch→buy 旧 html）。
- F-3 全市场扩池：**完成（2026-08-08）**——discover 复用优先（DB 新鲜跳过采集 + 失败重试一轮 + 进度落盘）之上补扩池执行：候选每类 20/总量 240（13 武器）+ 排除已库新鲜品 + 网络采集立即落库（INSERT OR REPLACE）；两轮扫描 items 110→136（+26 品，各补 90 日历史），第三轮扩容进行中（total=54）；被预筛跳过品也积累历史、下次重扫走 DB 复用；一天可手动多跑几轮。
- F-3.1 活跃池淘汰：**完成（2026-08-08）**——非自选/非持仓且最近 7 天平均在售量 <10 的品标记「活跃池淘汰」退出每日采集与 discover 捞回（数据保留，加回自选恢复采集）；首轮标记 7 品，正常采集池 166 品，保证 260 品价值靠活跃池质量而非数量堆砌。
- F-3.2 存量维护 + 链路台账：**完成（2026-08-08）**——不做增量扩池，收敛后存量更新（每日 18:00 K 线刷新已有）；新增 `pipeline/pool_log.py` 台账（daily/prune/discover 三类留痕，data/pool_maintenance_log.jsonl）+ `references/pool-maintenance.md` 完整链路手册；增量评估决策点 4 条（活跃池萎缩/新品骤降/K 线新鲜度/市场结构），数据积累后人工评估再决定是否扩池。
- F-3.3 文档分层：**完成（2026-08-08）**——数据层独立成册 `references/data-layer.md`（数据源/采集链路/调度表/更新维护/表结构/质量规则/故障 SOP，唯一权威）；根与子 AGENTS.md、PROJECT_STRUCTURE.md 数据章节压缩为引用，修正 PROJECT_STRUCTURE 过时运维口径（21:30→18:00）。
- F-3.4 discover 从池内跑：**完成（2026-08-08）**——默认 pool 模式扫池内活跃品（DB 复用零采集），search 扩池路径保留；扫描 181 品 / 70 通过 / 2 buy，见 v46。
- F-4 成本建模进回测 / F-5 组合模拟器：**待做（P1，研究层）**——待数据评估后议是否进引擎。
- F-6 事件驱动提示 / F-7 钉钉卡片一键执行：**待做（P2，体验层）**。
- F-3.5 高分品模块治理：**完成（2026-08-08）**——磨损过滤（仅崭新出厂）+ 流动性闸门（supply_depth<15 降级 buy），非 FN 品标记淘汰，见 decision-log。
- F-3.6 价格串品治理：**完成（2026-08-09）**——fetch_kline_90d 悠悠锚校验 + 82 品体检修正 7 品错价，见 decision-log。
- F-3.7 补仓/止损双路径：**完成（2026-08-09）**——五状态止损矩阵 + 倒金字塔 3:2:1 补仓（展示层建议，不触碰冻结参数），回测 data/stop_loss_backtest.json，文档 references/stop-loss-strategy.md。
- M-1 抛压衰竭大盘择时检验：**阶段 0 完成（2026-08-11）**——触发桶 sp>=85（n=18，去簇 7）win14 44.4% 与对照桶（drop20<=-7，n=54）完全相同，无区分度；触发集中于 2 个独立恐慌事件；n<30 只报告不判定，`market_th.py:526-537` 暂不调整，待独立事件>=3 复验；探针 `references/probe_selling_pressure_exhaust.py` → `data/_exp_selling_pressure_exhaust.json`。
- M-2 composite 消融检验：**完成（2026-08-11）**——TH 偏移方向坐实（spearman -0.344，TH<35 win14 80.8% vs TH>55 60.8%），反向变体（-1.0）Q5 win14 73.4% 最优；探针 `references/probe_composite_ablation.py` → `data/_exp_composite_ablation.json`；估值折价双计权未坐实，动作加分/数据质量乘子在 discover 全量候选场景另检（候选）。
- M-3 discover 发现榜 alpha 检验：**已立项（2026-08-11）**——追踪口径不扣 2%（`discover_tasks.py:494-545`）+ 市场弱过滤方向疑与「洗盘最优」张力 + 候选空间限 13 武器；检验设计见附录 E3；产出 `data/_exp_discover_alpha.json`。
- M-4 执行复盘口径量化：**已立项（2026-08-11）**——real 日历日 vs paper 交易日结算不一致（`main.py:1010` vs `signal_tracking.py`）+ action 混用（`dashboards.py:92-126`）；依赖 executions>=20（A1-4），检验设计见附录 E4。
- M-5 composite 展示层调整：**完成（2026-08-11）**——TH 偏移系数 1.0→-1.0（`item_analysis.py:674`，三件套：317 信号 / 反向 Q5 win14 73.4% vs 67.2% / spearman +0.015），测试断言同步（t_composite_score_fn 9.1→8.0），不落引擎。
- M-6 discover 候选扩展：**阶段 1 落地（2026-08-11）**——新增 `pipeline/item_categories.py` 品类识别，池内 discover 白名单扩展（崭新出厂 ∪ 印花/武器箱/挂件/收藏品/胶囊，印花×2+箱×2+挂件×1 进发现空间，角色×3 暂不入），results/`discover_history` 存档加 `category` 字段（M-3 品类分桶基础），展示层品类列 + 热力图分桶；引擎参数零改动；阶段 2（scan-all suggest 品类搜索词）待 csQAQ 稳定，阶段 3（品类 alpha 检验）与 M-3 合并。
- 三要素元数据入册：**✅ 完成（2026-08-12）**——`SignalFamily`（`item_analysis.py:1010-1107`）新增 hypothesis / counterparty / scenario / failure_signal 4 字段，4 个 buy 族全部填充 + 新建 `references/signal-family-registry.md` 注册簿（贴纸观察桶已按框架注册）+ PARAM_REGIME.iteration_note 同步；零决策参数。
- 去簇胜率主视图对照：**✅ 完成（2026-08-12）**——`dashboards.data_progress.dedup`（2% 口径 332 vs 29 信号 69.9% vs 58.6%）+ 进度卡 J-3「全量 vs 独立事件去簇」对照行；纯展示层，不动任何排序/决策。
- C 通道族级失效监测：**✅ 完成（2026-08-12）**——`j2_channel_monitor._channel_c_family` 族级月度下钻（n≥5 / 14d<60% / 30d<50% / 连续2月<70%，阈值入 `config.J2_THRESHOLDS`），输出 `j2_channel_status.json.channels.C.family_monitor` + dashboard「C 族级失效」行；当前观察 deep_dip 2026-06 与 deep_value 2026-07，仅提示不动作。
- 单参数改动前后半段一致性制度化：**✅ 完成（2026-08-12）**——`project-principles.md` 新增节：PARAM_REGIME 改动必填 前后半段一致性对照 + 轻量置换检验（≥200 次）；流程/文档项，零代码。
- 贴纸板块（**阶段 1 落地 2026-08-12**）——160 品全息战队贴纸清单（container_detail 全量枚举 18 胶囊）+ 158 新品入库（`source='sticker'`）+ **158 品 K 线全部落库**（11 品限流缺口 2026-08-12 手动补采完成，各 91 bar）+ 观察桶守卫落地（`item_analysis.py:decide_fusion_signal` 贴纸 buy→watch 强制降级，panic_resonance trigger 显式排除印花）；贴纸专用化**净效果**：守卫层禁 buy（周期因子/供给吸筹对贴纸仍计分，按 `first-principles-stickers.md` §3.4 待 A2 检验后决定是否代码层禁用）；阶段 2 = 品类分桶追踪积累（与 M-3 合并）→ A2 三件套后才可进 buy。

- 贴纸 alpha 研究排期（**2026-08-12 立项，见 decision-log「贴纸 alpha 研究立项 + 阶段 0 预跑」**）——四案例深历史验证后确认：贴纸 = 百倍级事件脉冲 + 尖顶形态（种子 12 品 9/12 尖顶，峰值回撤 67-99%），「低估区反弹 14d」A2 假设不适配。执行序 S-1 → S-2 → S-3/S-4（并行）→ S-5：
  - S-1（P1，数据前提）：160 品深历史全量回填研究快照 `data/_exp_sticker_deep_full.json`（**不进 price_history、不动引擎基线**；分批限流窗口 10-20 品/次，总量约 4-6 小时）；
  - S-2（P1，回测）：Major 日历周期择时正式三件套（160 品 + 真实赛程窗口；赛事季初买入/季末离场，net 2%，walk-forward）——阶段 0 预跑方向支持（W25 赛前均值 +574.7%、W26 春季 +116.5%、回落期 -35.8%）；
  - S-3（P2，研究）：战队竞技状态 proxy + 尖顶离场规则（sell-the-news）；
  - S-4（P2，流程）：贴纸 A2 检验假设修正（低估区反弹 → 事件驱动框架）；
  - S-5（P2，配置）：EVENT_CALENDAR 补 Major 赛程（用户配置；2025-11-20 布达佩斯贴纸上市已从数据确认 Major 在 11 月）。

- 发现高分品·贴纸独立 Top10（**2026-08-12 立项，P2 展示层**，见 decision-log 同条目）——结果栏「综合榜/贴纸榜」双 Tab，贴纸不与枪皮混排（sticker 108 品已占总榜，MOUZ 2021 全息 9.0 居首）；`discover_latest.json` 已含 category，纯前端过滤 + 渲染拆分，零决策参数。**双榜独立刷新（2026-08-12 落地）**：Top10 头部刷新拆「🔄 刷新综合榜 / 🔄 刷新贴纸榜」两按钮，scan-all 新增 `scope`（all/skin/sticker）按品类过滤池内 SQL；scope 刷新按 name 合并回 cache（另一榜保留、重渲染两榜完整 HTML），不写当天 discover_history（快照仍由全量扫描驱动）——见 decision-log「Top10 高分饰品·双榜独立刷新」，零决策参数。
- 贴纸庄盘专项（**2026-08-12 立项**，见 decision-log「贴纸庄盘专项 · 第一性原理研究 + H1 回测 + 庄盘指纹落地」+ `references/first-principles-manipulation.md`）——第一性原理确认贴纸天然适合坐庄（供给冻结/池小深度浅/Major 事件可预期），事件是庄家派发的确定性窗口；实证 142/144 品脉冲 + 尖顶崩塌 + 长期不复原（区间末中位剩峰值 18.7%）；H1 回测：V0 朴素尖顶负结果（3d 崩 50% 多为假摔）、V3 全局峰值后 30/90d 下跌胜率 100%（-50.5%/-67.2%，事后口径）。执行序：
  - 阶段 A ✅（2026-08-12，展示层零决策参数）：贴纸报告页「庄盘指纹」块（历史脉冲分位/尖顶形态/距峰值回撤/勿抄底提示/**D5 同届分化**）；
  - W-1 ✅（2026-08-12，regime 依赖不落地）：「峰值确认」离场（回撤≥70% + 峰值≥14d 前确认）confirm 7/14/21d 网格全时段仅弱有效（down30 56.6% med -4.4% vs 无确认 55.0%/-3.3%）；walk-forward 前段 +15.2%（踏空）vs 后段 -8.3%——固定参数不落地，regime 条件化变体待积累；产物 data/_exp_w1_peak_exit.json；
  - S-2 ✅（2026-08-12，负结果）：Major 日历周期三件套——当届贴纸上市即峰值无买点（64 品全参数网格负期望，最优 buy+7/sell+7 win 18.8% mean -28.7%；walk-forward 交叉全负 9-13%；当届/老贴纸赛事窗口对照均跌，老贴纸亦 -8.8%/-32.7%）；日历择时假设证伪，贴纸买点 = 庄盘选品；产物 `data/_exp_s2_major_calendar.json` / `references/s2_major_calendar.py`；
  - W-2 ✅（2026-08-12，评估完成不补采）：深历史在售量——fetch_simple_kline 无在售量、info/chart 含 num_data 可仿 simple 分页但 4-6 小时+限流风险；吸筹量确认仅近期触发有效（90d 已覆盖 2026-05 后），历史段无法事后验证 → 不批量补采，量确认走 90d 增量探索；
  - W-3（P1，回测，探索版完成）：「庄盘吸筹痕迹」左侧买点（Major 脱钩——用户确认贴纸与赛事已不挂钩）——深回撤>70% + 30d CV≤5% 超低波 + 企稳：66 事件/43 品，39 前视 win30 69.2% / med +18.2%（同期同形态 56.7%/+4.4%，置换 p_med 0.002）；**拉升承接方向证伪**（90d 在售三组全负、B2 横盘中继 win14 17.4%——贴纸无二波）；正式 A2 待 n 积累（8-14 起 pending 27 陆续落地）+ walk-forward 前半段补齐 + 在售量确认（90d 增量探索，supply 分层待积累）；产物 data/_exp_w3_accum_continuation.json；**跟踪已上线并挂每日任务** `references/w3_signal_tracker.py`（66 信号，run_daily_collect.py 每日刷新 → data/_exp_w3_signal_tracker.json）；**alpha 口径结论（v3）**：吸筹 = 相对市场选品 alpha（med +8.6%、82%>0；crash +7.4% / calm +9.3%），绝对收益依赖市场（calm +19.8% 可买 / crash -10.2% 禁买）——正式 A2 必含 regime 分层。
  - S-4 ✅（2026-08-12，流程/文档）：贴纸 A2 注册假设修正——Major 赛后低估区反弹 → 庄盘吸筹形态（含 regime 分层 A2 设计），signal-family-registry.md 贴纸段同步；
  - S-5 ✅（2026-08-12，收尾）：EVENT_CALENDAR 补 2025 双 Major 历史事件（2025-06-03 奥斯汀 / 2025-11-24 布达佩斯，type=major，仅回测外生标注，贴纸已脱钩不作买点依赖；2026 赛程待用户配置）；
  - S-3 ❌（2026-08-12 关闭）：战队竞技 proxy + sell-the-news 离场——Major 脱钩 + W-1 离场证伪后无立足点，不立项；
  - H2：庄盘拉抬判别探索版已并入 W-3B（90d 拉升承接三组全负证伪），正式版待历史在售量积累；
  - 数据修复 ✅（2026-08-12）：S-1 深历史遗留 seed 短名 2 品（G2/MOUZ 2024 上海）重命名为标准全名，庄盘指纹块对该 2 品恢复显示；
- A 通道计数口径核对（**2026-08-12，信息层**，见 decision-log「A 通道计数口径核对」）——`j2_channel_status.json` A.value=1 vs note「2 个独立事件」分叉的根因：A 计数源 = 回放信号派生事件簇（365d 窗口，2025-10 五合一滑出窗口），note 为手写死文本未同步；已改双口径标注（信号派生簇 1 / 市场独立事件 2）并重跑刷新状态文件，`A.value` 维持与 `display_keys.panic.events` 同源（t_j2_channel_status 硬校验）；**待办（P2 监测层，用户确认后实施）**：市场层恐慌事件台账（monitor_events 加 panic 类型 / EVENT_CALENDAR 补 2026-05 条目 + A 通道改读台账）。
- M-5 遗漏修复（**2026-08-12**
- 主引擎口径审计 + 评级线 th_boost 移除（**2026-08-12**
- 发现高分品结果行加数据采集时间（**2026-08-12**，见 decision-log 同条目）——`discover_latest.json` 结果行补 `collected_at`（DB 复用/联网采集/行级刷新三路径）+ 榜单名称列下「采集于」小字（与批量扫描同款）；纯展示层，冒烟 100/0/0。，见 decision-log「主引擎口径审计 + 评级线 th_boost 移除」）——全仓核对：评级切分/TH 三区/market_weak 45/半山腰/hold21/7天去重/去簇 gap 均一致；唯一真不一致 = value.score 后处理 th_boost（TH 高加分）与 M-5 反向定论矛盾且与 composite 对冲；回测先行（`probe_th_boost_grade.py`，317 信号：Q5 73.4→76.6%、spearman +0.087→+0.142、前后半段一致、置换 p=0.10）后**移除 th_boost**——TH 展示层仅由 composite_score th_bonus 反向单计；纯展示层，分级仓位/回放产物零影响；冒烟 100/0/0。，见 decision-log「M-5 遗漏修复：discover 行级刷新 TH 偏移同步反向」）——`webapp/main.py:1537` 内联复刻的 composite 公式 TH 偏移 `*1.0` → `*(-1.0)`，与 `item_analysis.composite_score` 对齐（M-5 反向定论）；discover 行级刷新与全扫/批量扫描综合分口径恢复一致；服务已重启，冒烟 100/0/0。

- 单品报告弹窗加载慢优化（**2026-08-12**，性能层/展示层，见 decision-log 同条目）：`/api/items/report-view` 加 6h 报告缓存（`analysis_results` 命中直接返回，持仓品跳过保留补仓/止损卡片）+ `market_snapshot` 5min 进程内缓存（批量扫描/大盘页共享，手动刷新后 bust）；实测报告弹窗 ~1.5s → 3-21ms、冷重建后二次点击秒开；`save_analysis_result` 补 `collected_at` 使缓存/discover 报告带「数据采集于」。零决策参数，冒烟 100/0/0。
- 生产信号跟踪恢复 + B-1 regime 分层聚合（**2026-08-12**，见 decision-log「生产信号跟踪恢复 + 对账挂接 + B-1 regime 分层聚合」）：A-1 实证=record_buy_signal 双挂接（`analysis_service.py:646` + `scan_tasks.py:187`）链路通，0 行系 items 重排 ON DELETE CASCADE 清空（commit `1060af7`，备份 `market.db.bak-before-reindex-20260811` 可证）；已回填 1 行（id 59→44）+ `reconcile_production_signals` 每日对账挂接（`data/signal_tracking_reconcile.jsonl` 留痕）+ data-layer.md §4 防护说明；B-1 由 B 升 A 线并落地：`references/expectancy_by_regime.py` → `data/_exp_expectancy_by_regime.json`（六态 × 族分层 n/win14/avg14/win30/avg30，回放字段齐备免重跑，`t_expectancy_sync` 未触碰）；Q7 暂缓（executions 仅 8 行）。冒烟 101/0/0。
- B-1 展示层落地 ✅（**2026-08-12**，见 decision-log「B-1 展示层落地：期望条按市场状态分层徽章」）：报告决策条新增「🧪 当前【状态桶】14d/30d」分层徽章（同族在当前市场状态桶下的 365d 回放分层，n>=5 展示、n<5 标注不足、产物缺失降级）；`_expectancy_badge` 加 state_bucket 参数 + `_load_expectancy_by_regime` mtime 缓存 + 模板紫色系徽章；纯展示层零决策参数，冒烟 102/0/0。


- A 线落地：A-3 今日关注+买点队列 / A-6 双口径桥接+进度条 / A-4 文案口径清扫 / A-7 组合闸门入仪表盘 / A-8 推送分层降噪（**2026-08-12**，展示/监测层，见 decision-log「A 线落地」）——A-3：watchlist 顶部「📌 今日关注」卡片（当日事件聚合 + proximity>=60 买点队列，proximity.score 主序 / bar_pct 次键 / 贴纸观察桶 / 7 日去重标签 / 采集时间）+ 报告按钮 + 红线提示；A-6：checkup 口径说明（回放告警=信息级 ≠ 实盘劣化）+ 执行区「录入 N/20」进度条；A-4：replay 页头死文本改 meta 注入 + 全仓硬编码口径清零；A-7：组合仪表盘加熔断状态（drawdown_status，实测 -44.65%/breaker_active）+ 单票敞口列（30% 阈值）；A-8：推送 danger 持仓置顶 + near_buy 降噪（计数+Top3+引 Web 今日关注）。零决策参数（阈值/评分/闸门全不动），冒烟 103/0/0，pyflakes 0，服务已重启。A-2 未动（Q7 暂缓）、A-5 discover 榜展示部分未做（watchlist 卡片已带观察桶徽章）。
- 吸筹族 chg7 门槛变体回测（**2026-08-12 研究，不落地**）——量化专家「边拉边吸」观点复核：cap=5/8 全引擎 avg14 不升反降（+15.87→+15.18→+14.73）、新增信号 60/74 集中于 2026-02 单段、「真·边拉边吸」（chg7 +3~+5）样本仅 1 条；维持 abs(chg7)<=3；研究开关 CS_ENGINE_SUPPLY_ACCUM_CHG7_CAP 保留（默认 3）；产物 data/_exp_supply_chg7_cap_{3,5,8}.json；详见 decision-log 2026-08-12 条目。
- 周期权重排序敏感性（**2026-08-12 研究，不调整**）——吸筹 2.0 vs 拉升 1.2 复核：cycle 分桶 win14 = 洗盘 75.2% / 吸筹 58.7% / 拉升 62.5%（avg14 +18.80/+9.19/+6.36）；accum 权重降档（2.0→1.5/1.2/1.0）排序 spearman 0.298→0.303 无改善、Q5 恒 82.0%；当前权重次序维持，文档注明「吸筹胜率最低但期望非最低」口径细节。

## 立项排期（2026-08-13）

### EXIT-1 · 路径依赖退出策略（P0，阶段0完成）
- 阶段 0：2026-08-13~15 只读探针；正式 A2：2026-08-17~21；判定：2026-08-24。
- 阶段0：hold21 优于简单 trailing；组合 Calmar 7.68 vs 3.90~4.66，方向为负。

### LIQ-1 · 流动性/成本感知仓位（P1，阶段0完成）
- 阶段 0：2026-08-13~15 成本阶梯；影子展示：2026-08-24~10-30；正式判定：2026-11-09~13。
- 阶段0：2% 基线 win 71.0%/+15.95；低在售桶样本过少，200+ 桶明显更优。门禁：spread 连续 90 天 + executions >=20。

### SUPPLY-CONF-1 · 供给收缩机制分离（P2，阶段0完成）
- 阶段 0：2026-08-13~15 数据盘点；积累：2026-08-24~10-30；正式判定：2026-11-09~13。
- 阶段0：供给吸筹族 142 条仅 11 条信号日 in_sale>0；干净段仅 3 条，正式 A2 不可启动。门禁：连续 in_sale 90 天 + spread 90 天 + monitor_rank 周频 12 周。
- 观察层：`probe_supply_bid_observer.py` 已做供给收缩×价格平稳影子候选，与 BID-1 共用，不接 buy。

## 补充立项（2026-08-13）

### PORT-1 · 组合风险预算与资金分配（P0）
- 阶段0完成（2026-08-13）：vol/regime/vol_regime 均未改善 Calmar/maxDD，p=0.18；不进入正式 A2，维持静态 cap0.8。
- 与 EXIT-1 互补：一个决定何时退，一个决定投多少；后续若重开须等 in_sale 连续段与真实成交/滑点样本。

### PANIC-ALIGN-1 · 恐慌族回放/生产情绪一致性（P1）
- 阶段0完成（2026-08-13）：真实贪婪情绪 vs 价格近似情绪 spearman +0.118、`sent>=75` 一致率 46.7%；回放 panic 92 条全部为近似口径。只报告不判定。
- 后续：`signal_tracking` 乱码已修复；继续等生产 panic≥20；数据门禁 production>=20。

### BID-1 · 求购因子验证（P2）
- 与 SUPPLY-CONF-1 共用观察跟踪器，积累 bid/spread，不接 buy。
- 观察层已落 `_exp_supply_bid_observer.json`：343 条求购字段，最新唯一候选 4 品暂无求购数据；等 spread/bid 与供给收缩候选重叠积累。

### STICKER-1 · 贴纸庄盘专用策略（P2，2026-08-13 放置：主引擎优先）
- 2026-08-13 放置：W-3 每日跟踪保留但暂不投入，正式 A2 暂缓；主引擎工程健康与量化待数据项优先。

### EVENT-1 · 外生事件日历与供给事件提示（P2）
- 仅提示层；等用户配置 Major/开箱/新箱未来日历。

### CROSS-1 · 跨平台价差/流动性迁移（P2）
- 先只读统计与展示；多平台采集须轻量化。

### 整合梯队
- 第一梯队：EXIT-1 / PORT-1 / PANIC-ALIGN-1。
- 第二梯队：LIQ-1 / SUPPLY-CONF-1 / BID-1；STICKER-1 已放置。
- 第三梯队：EVENT-1 / CROSS-1。

## 产品侧下一版迭代方案（v57，2026-08-13）

> 前提：A-9 贴纸停采停扫已落地，引擎侧进入“等数据采集验收”；产品侧本轮只做展示/执行体验/监测口径，不碰引擎参数、信号族、闸门、权重、阈值。

### P0 · 近 2 周可做（零引擎参数优先）
- **P0-1 A-9 落地验收 + 采集预算回收观测（1 天内）**
  - 次日 18:00 全量采集后核验：`items.notes` 命中「贴纸模块停采」= 159、自选贴纸 1 品豁免；`run_daily_collect.py` 活跃池不含贴纸；`/discover` 无贴纸榜、旧 `scope=sticker` 已转 `skin`。
  - 在 `/checkup` 或数据积累进度卡增加一行只读口径：采集池 = 主引擎 201 品 + 自选豁免品；说明贴纸回收的 csQAQ 预算转投主引擎 K 线/在售量。此为展示口径，同步 decision-log 一行，不触发回测。
- **P0-2 A-2 执行环轻推（买入侧闭环，2-3 天）**
  - 目标：补上“今日关注/buy/watch → 已建仓/未建仓/补仓”的执行闭环，驱动 `executions` 从 8 向 20 积累，逐步解锁 A1-4 滑点/成本校准。
  - 内容：单品报告与 watchlist 执行区显示「最近建议 + 已记录状态」；7 天未决策/未记录的 buy 类条目在 watchlist 顶部提醒；参考价一键回填执行录入（数量/成交价/滑点）。
  - 边界：不接 `advice_id` 回链（Q7 已暂缓，样本不足），执行记录保持自由文本；不改信号/仓位/阈值。
- **P0-3 生产 buy 首条观测 + C 通道口径（0.5 天）**
  - `/checkup` 的 C 通道把当前 1 条生产信号标注为「供给吸筹族，非 panic」，避免与 PANIC-ALIGN-1 混淆；展示「生产样本 1/20」并提示继续等事件与录入。
  - 复用 `signal_tracking` / `j2_channel_status`，零引擎参数。

### P1 · 可并行或等待触发
- **P1-1 执行录入反哺成本校准（A1-4 前置体验）**：录入页预填/校验滑点与成交价；`N/20` 进度条已有，等 `executions≥20` 后再切换 2% 成本假设，本轮不提前改。
- **P1-2 采集健康/预算回收看板（可选，只读展示）**：若引擎侧已暴露 G-4 失败率，将贴纸回收后的主引擎覆盖/失败率并列展示；无新采集字段则不做。
- **P1-3 B-4 钉钉 action 卡片预研（体验线，不接执行）**：A-8 已降噪，先完成资金安全设计与 advice→exec 回链方案，等 `executions≥20` 再审；不做无确认下单。

### P2 · 明确不排期 / 等数据
- **B-2 求购因子、B-5 成本建模、B-7 买点队列 v2**：维持“等 90+ 天样本 / `executions≥20` / A2 前置”，本轮不排期。
- **STICKER-1 / W-3 / S 系列贴纸 A2**：已冻结；静态研究资产保留，不重启、不物理删除。

### 过程指标（本版验收）
- 执行记录：7 天内 `executions` 从 8 增至 12+，且出现 ≥1 条买入侧录入。
- 采集验收：贴纸 0 采集、主引擎 201 品 18:00 全量成功率 ≥95%（沿用 G-4 口径）。
- 生产样本：`signal_tracking` 新增 buy 记录正确入账，0 乱码。

### 与引擎侧并行的边界
- 引擎侧量化项：EXIT-1 正式 A2（2026-08-17~21，判定 2026-08-24）；LIQ-1 / SUPPLY-CONF-1 / BID-1 数据积累（判定窗口 2026-11-09~13）。产品侧只提供录入/口径/样本，不干预判定。
- 纪律：本方案任何展示口径变更同步 `decision-log.md` 一行；任何引擎参数/信号规则变更仍走回测先行 + 三件套 + A2。
- 北极星不变：连续 30 天有新增执行记录（含买入/补仓），最终解锁 `executions≥20`。

## 数据储备 API 排期（v58，2026-08-13）

> 来源：`references/data-reserve-api-audit.md`；数据能力线，与 v57 产品体验线并行。所有新增数据只进研究层，不接评分/决策；接引擎另行 A2 + 三件套。

### P0 · 活跃池基本面 + 求购历史（第一周 dry-run，第二周灰度）
- 新表：`item_fundamental_snapshot`、`bid_history`（日聚合，`platform=2`）；`survive_history` 归 P1。
- 请求预算：约 202×3=606 次/日，错峰 18:00 K 线；429 退避 + 台账。
- 门禁：连续 3 天覆盖 ≥95%、平台标记 100%、不写引擎表。
- 落地（2026-08-13）：`collect_data_reserve_p0.py`；`item_fundamental_snapshot` 202 品、`bid_history` 18,381 行；备份 `data/backup/market_20260813_222205.db`。

### P1 · 存世量 + 集中度 + 系列面板（第三周起）
- `survive_history` 先接自选/持仓；`series_snapshot` 每日一次；`monitor/rank` 周度 Top20/50，隐私最小化。
- 门禁：自选/持仓覆盖 100%，存储增长符合 `data-layer.md` 增长预算。
- 落地（2026-08-13）：`collect_data_reserve_p1.py`；`survive_history` 195 个有效品×180天=35,100 行（7 个武器箱/角色/挂件/印花类 API 空序列）；`series_snapshot` 64 行；`monitor_rank_snapshot` 自选/持仓 43 品 Top20=860 行；备份 `data/backup/market_20260813_231242.db`。
- 覆盖口径：自选/持仓 43 品中 39 个有效品 100%有 180 天序列；4 个非枪械/非皮肤品 API 返回空序列，不计入覆盖缺失。

### P2 · 全市场储备（暂缓）
- 等 P0/P1 稳定 30 天 + 产品确认全市场表规模/预算，再立项。

### 引擎接入边界
- BID-1 / F-2 / SUPPLY-CONF-1 / A1-4 的样本门禁不变；数据到位只缩短等待，不自动接 buy。
