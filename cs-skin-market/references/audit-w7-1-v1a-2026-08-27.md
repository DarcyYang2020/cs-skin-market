# ③独立审计 · W7-1 v1a（v0+bid+spread）评估复核（2026-08-27）

**审计对象**：②v1a 执行（decision-log FF + `data/_exp_emotion_v1a_2026-08-27.json` + 研究脚本 `references/run_emotion_v1a_eval.py`）
**审计员**：窗口③（独立审计 / 刹车）
**红线执行**：只认 W7-1 v1a 预注册判据（`references/w7-1-v1a-emotion-prereg-2026-08-27.md`，FC 冻结）+ 产物事实；FF 自述仅对照。**独立复算核验**（审计脚本 `references/_audit_v1a_recompute.py`：独立实现 Spearman/平均秩/OLS 残差/双口径增量 IC，恐慌分/bid/spread 复用生产数据链路；产物 `data/_audit_v1a_recompute_2026-08-27.json`）。

---

## 一、判据口径对照（预注册 §1/§2/§5 vs 执行实现）

| 项 | 预注册（FC 冻结） | 实现（run_emotion_v1a_eval.py） | 对照 |
|---|---|---|---|
| v1a 合成 | `emo_v1a = clip(emo_v0 + 0.5·bid_norm + 0.5·spread_norm, 0, 100)`，w1=w2=w3=w4=0.5 固定禁优化 | 同款（常量权重，无优化路径）✅ | ✅ |
| 归一化 | bid_norm/spread_norm 逐日截面 rank（0-100，R5 同规则） | 同款（fit/val 同规则）✅ | ✅ |
| 判据 A | 对核心 6 因子正交化 → 增量 IC≥0.02 **且** 同号月≥80% | `inc_ic_daily(EMO_V1A, FIT)` + period_of 过滤月度滚动 | ✅（滚动口径见登记 1） |
| 判据 B | 对核心 6 **+ emo_v0** 联合正交化 → 增量 IC≥0.02（无 rolling 要求） | `inc_ic_daily(..., extra_cols=[EMO_V0])` ✅ | ✅ |
| verdict | **A 且 B 均过 → 候选**；任一不过 → 无增量 | `mA≥0.02 AND ssrA≥0.80 AND mB≥0.02` | ✅ |
| 守院 | 探索仅 fit；val 仅复验；require_fit 接线 | fit 逐行 require_fit（D6）；val 仅 inc_ic_daily 复验 | ✅ |

## 二、独立复算核验（10 项全 PASS）

| 核验项 | 复算结果 | ②产物 | 一致 |
|---|---|---|---|
| 组件 sentiment / sc30 / chg7 | 0.1579 / 0.0215 / −0.1457（含 coverage/rolling 全字段） | 同 | ✅ |
| 组件 bid / spread | **+0.0101 / −0.0628**（coverage 0.8693，与 R1 组9 同量级弱/无效） | 同 | ✅ |
| emo_v0 IC14（参照） | 0.1264 | 同（R5 一致） | ✅ |
| emo_v1a IC14 / IC30 / coverage / rolling | 0.1019 / 0.0768 / 0.8523 / 0.8333 | 同 | ✅ |
| **判据 A 增量 IC（fit）** | **+0.0034 / n 373** | 同 | ✅ |
| **判据 A 滚动同号月** | 16 个月 / 0.50（period_of 口径） | 同 | ✅ |
| **判据 B 增量 IC（fit，相对 v0）** | **+0.0107 / n 409** | 同 | ✅ |
| **val 复验 A / B** | **−0.0052 / −0.0030**（反向印证） | 同 | ✅ |
| verdict | 无增量 | 同 | ✅ |
| meta（fit 156,418 / val 83,408 / split） | 同 | 同 | ✅ |

**verdict 判定复核**：判据 A 需 IC≥0.02 **且** 同号月≥0.80 双过——实测 IC 0.0034（远低）且 rolling 0.50（<0.80）**双不过**；判据 B 需 IC≥0.02——实测 0.0107 不过。**A 且 B 均须过才候选，均未过 → verdict=无增量 判定正确** ✅。

## 三、守院核验（通过）

| 项 | 核验 |
|---|---|
| oos_zone | fit 行逐行 `require_fit`（D6，恐慌分 + bid/spread 两处接线）；val 仅预注册 §5 增量 IC 复验触碰 ✅ |
| 权重固定 | w1=w2=w3=w4=0.5 常量，无优化路径（权重变化=新预注册）✅ |
| 仅守卫/加分 | 脚本只读矩阵/回放库/生产库；产物仅 `data/_exp_emotion_v1a_*.json`；未碰引擎、未 bump ENGINE_VERSION ✅ |
| 候选来源 | bid/spread 未进引擎 = 独立扫描（不照镜子 C2）✅ |
| 确定性 | 无随机性 ✅ |

## 四、登记（2 项，不影响 verdict）

1. **判据 A 滚动同号月口径**（与 R5 审计 DZ 登记同款）：月度滚动仅统计 `period_of`（市场上下文可算）日期 → 16 个月/0.50；审计全量可算口径 months 会更多、ratio 可能不同。但判据 A 的 IC 0.0034 < 0.02 已不过（与 rolling 独立），**verdict 不依赖滚动口径**。R5/v1a 两处同口径，建议后续预注册统一明确（全量 vs period_of）。
2. **v1a 全 IC 0.1019 略低于 v0 0.1264**（bid/spread 加入稀释恐慌分主信号）+ 判据 B 0.0107 接近但不达——**bid/spread 在 v0 之上无净增量**，与诚实预期一致（单组件弱/无效 + v0 无增量 → 合成无增量）。

## 五、裁定

- **v1a 复核通过**：无增量证伪登记成立——判据 A（0.0034<0.02 且 0.50<0.80）与判据 B（0.0107<0.02）均未过，val 复验反向印证；verdict 与产物一致。
- **R5 + v1a 合并口径**：内生情绪 v0/v1a（恐慌分 ± 供给/动量 ± 盘口 bid/spread）**整体无新增信息量**，登记 registry 防重复挖；v1b（+turnover）/v1c（+steamdt）数据到位后重新预注册（判据 §6 声明）。
- 未触发 §4 四关（verdict=无增量，四关仅候选触发）✅ 符合判据。
- 不踩红线：仅筛查层（登记防重复挖，不立落地卡）；审计未替②调参、未改判据到通过。无生产改动、不 bump ENGINE_VERSION ✅。
