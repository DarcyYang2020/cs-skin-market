# ③独立审计 · CE 族开回放 + delta 清单 + A2 四关（2026-08-20）

**审计对象**：②研究窗口 CE（两候选族 bull_steady / crash_vol 的族开回放 + delta 清单 + A2 发射分布复算）
**审计员**：窗口③（独立审计 / 刹车）
**红线执行**：只读原始产物 + 预注册判据 + 工具定义；②自述（CE 正文）仅作对照定位产物，**结论全部由原始产物独立重算支撑**。
**独立重算脚本**：`references/_audit_ce_verify_2026-08-20.py`（managed python 3.13 直读 45MB 产物重算 delta/分段统计，并重跑 `a2_emission.analyze` seed=0 复算五门）。

---

## 一、审计对象复述

| 交付物 | 路径 | 性质 |
|---|---|---|
| 族开全量回放 | `data/_exp_family_variant_replay_2026-08-20.json` | 原始产物（13,865 信号） |
| bull-only 回放 | `data/_exp_family_bull_replay_2026-08-20.json` | A2 复算输入（13,644 信号） |
| delta 清单 | `data/_exp_family_delta_2026-08-20.json` | ③CD 硬验收项 |
| A2 复算 | `data/_exp_a2_bull_steady_2026-08-20.json` | 五门裁定产物 |
| 研究脚本 | `references/run_family_variant_replay.py` | CD 约束落实载体 |
| 预注册 | `references/family-candidates-prereg-2026-08-20.md` | 跑前锁定的 trigger + 注入口径 |

## 二、方法学核验（通过）

1. **研究脚本忠实 CD 约束**：复用 `scripts-archive/run_item_backtest.py` 的 `backtest_item` 内核 + `pipeline.backtest_common.build_market_context`；运行时注入两新族（`SIGNAL_FAMILIES` tuple 重赋值 + `SIGNAL_FAMILY_BY_KEY` 重建 + `_POST_FAMILIES` 重建 + `DEDUP_PRIO_BY_LABEL` + exec 补丁买涨腿硬编码元组尾加 bull_steady）；独立输出（`CS_ENGINE_FAMVAR_OUT`）；含 3 品 smoke 字节一致核对。**与 CD 逐条吻合**。
2. **红线实证**：`git status` 本轮 **零 `pipeline/`、零 `webapp/` 改动**（仅有 data 运行时产物与 references 文档；commit `bb5f2df`=CE、`cc83e69`=CC 两处更正）→ 「不改生产代码」成立。
3. **trigger 边界与预注册逐字一致**：bull_steady `vol30≤79.4 ∧ −7.3<mchg7≤4.1 ∧ drop21>3.7`；crash_vol `vol30>79.4 ∧ vol7>421.2 ∧ mchg7≤−7.3 ∧ drop21>−48.4`（drop21 即 mchg21 同符号映射，口径自洽）。

## 三、delta 独立重算（③CD 硬验收，全部复现）

| 项 | CE 自述 | ③独立重算 |
|---|---|---|
| baseline / fam_on 信号数 | 377 / 13,865 | 377 / 13,865 ✅ |
| added | 13,500 | 13,500（bull_steady 13,279 + crash_vol 221，**knock-on 0**）✅ |
| displaced | 12 | 12（matched=365：352 未变 + 13 relabeled）✅ |
| **net 漂移** | **0** | **0**（365 条 matched 信号 net14/net30 逐条字节一致，注入无数值污染）✅ |

→ **注入机制验证成功**：研究脚本变体可行，且未污染既有信号数学。

## 四、crash_vol：A2 否决线自动驳回 —— 确认（③CC 预注册）

- 独立重算：**n=221，单月 2025-10 占比 100.0% > 50% 否决线** → 自动驳回，不得按族落地。
- CC 预判（91.6% 挤 2025-10 单事件簇）实锤且比预判更极端（100%）。符合治理「单簇>50%→警告」红线。

## 五、bull_steady：A2 发射复算 FAILED —— 确认

③独立重跑 `a2_emission.analyze(bull, baseline, regime="all", seed=0)`，五门与产物**逐字复现**：

| 段 | added | 买书对照 | p_avg |
|---|---|---|---|
| fit | n=9,173 | win14 46.6% / avg14 **+1.71** | win14 55.2% / avg14 **+1.88**（n=116） | 0.936 |
| val | n=4,106 | win14 **49.1%** / avg14 **+5.99** | win14 **78.9%** / avg14 **+25.07**（n=261） | **1.0** |

五门：`n_val≥15` ✅（4,106）｜`val_avg14_excess≥2pp` ❌（5.99−25.07=−19.1）｜`val_win14≥book` ❌（49.1<78.9）｜`fit_val_direction` ❌（两段 avg14 均低于买书）｜`p_avg<0.05` ❌（1.0）→ **FAILED**

**语义判读（与②结论独立一致）**：扫描发现的牛市上行段「正期望区域」真实存在（引擎欠覆盖成立），但作为**宽触发族**（13,279 条，是基线 377 条的 35 倍）进引擎会**稀释高质量买书**——引擎现有选择性（374 条高期望信号，val win14 78.9%）远优于 13k 条平庸信号（49.1%）。四关正确拒绝。

## 六、裁定

- **CE 审计通过（驳回确认）**：两候选均正确被 A2 硬门槛拒绝，**无落地项**。②执行干净——研究脚本变体成功（net 漂移 0）、产物完整（含 bull-only 复算输入）、数字与独立重算逐字一致、预注册 trigger 与注入口径被严格遵守。
- **无追溯修正项**。本轮闭环结论：族划分重构的「新增宽触发族」方向被证伪（正确证伪，非证据不足）。
- **未来方向（非本轮、非落地）**：若再探索 bull_steady，须「高选择性」窄化（使信号量级与买书可比），不得宽触发；且须走完整预注册 + 样本外/live 链路。crash_vol 单事件簇无泛化价值，不再追。

---

**一句话**：③独立重算逐字复现 CE 全部数字——crash_vol 单月 100% 触预注册否决线自动驳回、bull_steady 13,279 条宽触发稀释买书（val win14 49.1% vs 78.9%，p_avg=1.0）五门 FAILED；**两候选驳回确认，无落地项，CE 审计通过**。
