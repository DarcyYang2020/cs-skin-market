# ③独立审计 · Wave1 数据地基 D1–D7 落地（2026-08-27）

**审计对象**：研发 Wave1 D1–D7 落地（decision-log DH + roadmap v82 Wave1 卡 825–869 行）
**审计员**：窗口③（独立审计 / 刹车）
**红线执行**：只认 roadmap v82 卡内**预注册判据 + 验收标准** + **代码/产物事实**；DH 自述仅对照。**独立重跑冒烟 140 passed / 0 failed / 0 skipped** + 不变量套件独立 4/4。

---

## 一、逐项核验

| 项 | 预注册判据核验 | 验收核验 | 判定 |
|---|---|---|---|
| **D1 price_source 列** | 幂等迁移（db.py:286-290）；写入集中 collector（save_price_history_batch:808 缺省回退 PRICE_SOURCE_DEFAULT）；不重建历史 | 迁移幂等 ✅；冒烟 ✅；**锚定优先级冲突（见二.1）** | 有条件通过 |
| **D2 卖侧列** | 幂等迁移（bid_history lowest_sell/sell_count，db.py:630-641）；save_bid_history 写列；沿用 bid_history；日采累积 | 迁移幂等 ✅；冒烟 ✅；首周非空率低为预期 ✅ | 通过 |
| **D3 cleaning_ledger+规则配置化** | CLEANING_RULES 段（batch_guard/锚定/污染/freshness 带出处+生效日）；触警 append（run_daily_collect:144-147）；健康检查读计数（run_health_monitor:42-45）；阈值外置（batch_guard 由 run_daily_collect:129 从 config 读） | config 可加载 ✅；**无专门冒烟用例覆盖「人为触发→LEDGER+计数」**（测试覆盖缺口，功能链路已核完整）；8/10 机制经 config 阈值执行不回退 ✅ | 通过（附测试建议） |
| **D4 provenance+恢复演练** | provenance.py append；drills 三脚本存在；每月演练落台账 | **rebuild_derived 未实跑**（DH 明示避免干扰②R1 进行中回放库）→ 验收②「幂等跑两次一致」**未实际验证**；recovery_drill 已实跑（recovery_drill_log.jsonl 01:04 记录，market/replay_hybrid/replay_cycle_win 三库 integrity 全 ok + 行数留痕 PASS）✅ | 有条件通过 |
| **D5 数据不变量** | tests/test_data_invariants.py；schema/值域/主键/日期用例；self_test 注入坏行（price_rmb<=0→失败）；接入每日任务收尾钩子 | 独立套件 4/4 ✅；注入坏行→失败机制 ✅；冒烟计入 ✅ | 通过 |
| **D6 oos_zone** | config.OOS_ZONE（val_start=2025-08-10/b_channel_start=2027-04-25，config.py:456）；oos_guard.require_fit（fit 放行/val 无预注册 raise）；仅元数据不删数据 | 定义可读 ✅；**oos_guard 零研究脚本消费**（references/*.py 无 import，仅自身+测试）→ 验收②「研究回放脚本触碰报错」**未达成**；B 通道标记 ✅；冒烟 ✅ | 有条件通过 |
| **D7 raw.db** | raw_db.py 三表（raw_order_book/raw_trade/raw_survive）append-only（仅 CREATE TABLE，无 UPDATE/DELETE）；接线 collect_data_reserve_p0/p1；git 不跟踪 | 无变更 SQL 源码级 ✅；**raw.db 文件未生成**（今日采集未跑，凌晨正常，代码就位待采集写入） | 通过（文件待生成） |

## 二、须 PM/研发登记的事项

1. **D1 锚定优先级冲突（实质，须 PM 定口径）**：`config.PLATFORM_PRICE_SOURCE={2:yyyp,1:buff,3:c5}`+`PRICE_SOURCE_DEFAULT=yyyp`（**yyyp 优先**，与 AGENTS.md「定价锚: 悠悠有品 > Buff > C5GAME」一致）；但架构文档 `cs-quant-architecture.md §1.1` 表写「定价锚 buff>yyyp>c5>steam」。**同一口径三处两版，单一事实源三铁律（D1 目的）自身被违反**。代码/AGENTS.md 与实际采集一致（yyyp 优先），架构文档过时——须 PM 定口径 + 更正架构文档 + decision-log 登记。
2. **D4 rebuild_derived 未实跑**：验收②（幂等跑两次一致）未实际验证（compile+演练+08-20 同款脚本佐证）。登记为「待②R1 稳定后补跑验证」，roadmap 卡状态行不得写「验收全过」。
3. **D6 oos_guard 未接线研究入口**：验收②未达成——模块与测试在位，但 references/*.py 零消费。须在 Wave2 研究入口（R1 回放）接线时落实 `require_fit`（否则反过拟合硬约束落空）。
4. **D3 测试覆盖缺口（建议）**：补一条「人为触发 batch_guard → cleaning_ledger 有记录 + health_monitor 计数 +1」冒烟用例（验收②显式覆盖）。
5. **D7 raw.db 待生成**（非缺口）：采集运行后生成，届时复查三表写入路径。

## 三、裁定

- **D1–D7 全部通过（或有条件通过）**：代码实现忠实卡内预注册判据；冒烟独立重跑 140/0/0 ✅；不变量套件独立 4/4 ✅；D5 的 self_test、D7 的 append-only 源码断言、D3 的 batch_guard 阈值外置链路均经独立核验成立。
- **四项须登记**（二.1–二.4）；其中 **D1 锚定优先级冲突**与 **D6 oos_guard 未接线**为实质项（分别影响口径事实源与反过拟合落地），须 PM 定口径 / Wave2 接线时落实；D4 补跑验证、D3 补用例为收尾项。
- 不 bump ENGINE_VERSION（数据层）✅；recovery_drill 演练留痕符合 D4 台账格式（JSONL 机器可读）✅。

---

**一句话**：D1–D7 代码实现忠实预注册判据、冒烟独立重跑 140/0/0、不变量套件 4/4，全部通过或有条件通过；须登记四项——D1 锚定优先级三处两版（代码/AGENTS=yyyp 优先 vs 架构文档=buff 优先，PM 定口径+更正架构）、D4 rebuild_derived 未实跑（验收②未验证，待 R1 稳定补跑）、D6 oos_guard 未接线研究入口（验收②未达成，Wave2 落实）、D3 补人为触发用例。
